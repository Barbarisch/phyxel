# vault / strongroom — Room Data Sheet

> Schema: [`README.md`](README.md). Used by: bank, civic_palace treasury. Reuses the `bank` archetype sheet. **The secure core (T2).**

## Function
Secure storage of money/valuables. Most-secure.

## Required fixtures (→ #17)
- An **iron vault door** + **deposit boxes / strongboxes**.

## Typical fixtures (→ #16)
- Shelving; (scaling) a bullion room within.

## Service
- **One** door; **no exterior window**; the **floor and ceiling** also meet the vault spec (tunnelling threat); a guard sightline on the door.

## Adjacency
- Reached **only through the counting room** (staff, T1) — never the public hall; core-of-plan or below grade.

## Dimensions
- Walls **2–4 m** (fortified) / ≥ 0.667 m (stone) — REUSE the keep/retaining canon (cited, `bank`); room size `to_ground`.

## Function testers
- **F1** One iron door; no exterior window.
- **F2** **Every** wall + the floor + the ceiling meet the vault spec (tunnelling).
- **F3** Reachable only through controlled staff space (no public→vault path).
- **F4** A guard sightline on the door.

## Grounding ledger
- Wall thickness (keep/retaining), full-envelope spec, access rule — REUSE `bank` (cited); room size `to_ground`.
