# Ravenmere — the "make a full short game" gap ledger

**Purpose.** Ravenmere (`PhyxelProjects/Ravenmere`, port 8104) is a deliberately complete
*short* Baldur's-Gate-like: story-driven, D&D turn-based combat, several distinct characters,
one engine-generated town, multiple quests, companions, a dungeon, save/load, a real ending.
It exists to find **what the engine + pipeline are missing** when you try to ship an actual game,
not to demo any one feature. Every hole we hit is a row here — with how it bit, the workaround
used (if any), and what the real fix is. Rows are dated; nothing is deleted, rows get a
**Status** update when fixed.

Companion docs: `StandaloneParityGaps.md` (the Hearthvale increments this builds on),
`README.md` (the production workflow), the project's own `GAMEPLAN.md`.

Severity: **S1** blocks the BG-like loop for a human player · **S2** forces an authoring
workaround that degrades the design · **S3** friction / polish.

## Design of the game (so the gaps have context)

- **Scenes:** `main_menu` → `town` (Ravenmere, engine-generated village) ⇄ `cellar` (tavern
  rats) · `town` ⇄ `farm` (Hollin Farm, wolves, the hermit's mill) ⇄ `barrow` (the Hollow
  Barrow: antechamber fight, crypt boss).
- **Cast:** Reeve Aldric (main quest giver), Priestess Maera (lore + alternate ending),
  Hobb (tavernkeeper, rats quest), Selby (merchant), Farmer Dunstan (wolves quest);
  companions **Bram** (fighter), **Wren** (rogue, in the stocks), **Oswin** (wizard hermit);
  villains the Hollow Priest + cultists + skeletons.
- **Quests:** main arc *The Missing* (6 objectives, branching ending: relic to the Reeve or
  broken on Maera's altar) + 4 side quests (rats, poacher, wolves, grimoire).
- **Player:** authored pre-made human cleric (see gap G-01).

## Gap rows

| # | Sev | Area | What we needed | What exists | Workaround in Ravenmere | Real fix | Status |
|---|---|---|---|---|---|---|---|
| G-01 | S1 | Character creation | Class/race/ability pick at game start (rpg template milestone `character_creation`, req L4) | Nothing. `progression.class/race` is a single authored pre-made (`create_project.py` progression block). `ClassDefinition` documents skill-choice counts nobody reads. | Pre-made human cleric | A data-driven character-creation screen (menu-scene panel → writes the sheet) or at minimum a menu that picks among authored pre-mades | OPEN 2026-09-08 |
| G-02 | S1 | Combat input | A human player must be able to **End Turn** | `PlayerTurnController::endTurn` exists; the default HUD ships an End Turn button, but only the editor binds its onClick. The scaffold never calls `endTurn()`; AI waits on the human forever. Encounters only resolve by killing everything. | None yet — a fight where the player wants to hold position cannot progress | **FIXED in `create_project.py` 2026-09-08**: End Turn button bound (mirrors `Application::bindCombatHudButtons`) + **Space** ends the turn (edge-triggered). **L4 run 5**: `End Turn (Space)` ×9 in the shipped log, turns advanced each time | FIXED (L4) |
| G-03 | S1 | Dialogue | Skill checks in dialogue (`[Persuasion DC 15]` gates) — the BG signature | `DialogueSkillCheck` is complete + unit-tested but referenced nowhere: not a `DialogueChoice` field, `DialogueSystem` unaware, no JSON schema | Wren's "persuade the Reeve" is a story-variable gate flipped by another node | **FIXED in the engine 2026-09-08**: `DialogueChoice.skillCheck {skill|ability|type, dc}` + `failNodeId`; the choice text carries the check's label; `DialogueSystem::selectChoice` rolls d20 + a host-wired bonus (`setSkillBonusResolver`, the scaffold answers from the player's `CharacterSheet`), branches pass/fail, emits `dialogue_skill_check {roll,bonus,total,dc,passed}`. Ravenmere: `[Persuasion DC 12]` release Wren, `[Persuasion DC 10]` free drink. **L4**: run 6 `roll 7 + 0 = 7 vs DC 12 -> FAIL` (refuse node), run 7 `roll 17 + 0 = 17 vs DC 12 -> PASS` (Wren freed early, observation trigger fired) | FIXED (L4) |
| G-04 | S1 | Party | Control companions in combat (select, order, or switch) | `Party` is a ledger; companions follow (`behavior:"follow"`) and fight autonomously via `CombatAISystem` | Companions are AI-only allies | Party-member turn control through `PlayerTurnController` (bind the TurnActor to the active member) + a portrait bar | OPEN 2026-09-08 |
| G-05 | S2 | Enemies | SRD stat blocks (AC, HP, attacks) for authored enemies | `monsterId` is settable only via MCP `spawn_npc`/`spawn_encounter`; `game.json` NPCs only take `maxHealth`; both sides use pseudo-AC `8+floor(HP%*6)` | Rats/wolves/skeletons authored as bare `maxHealth` | **FIXED in the engine 2026-09-08**: `GameDefinitionLoader` reads `npcs[].monsterId` (loads `resources/monsters` on demand, sets the stat block's average HP unless `maxHealth` overrides, logs `NPC 'x' uses stat block 'y' (AC, hp, CR)`); `PlayerTurnController::pseudoAC` and the enemy AI's target AC now use the stat block's real armor class. Ravenmere's 13 enemies are SRD `giant-rat`/`wolf`/`dire-wolf`/`skeleton`/`cultist`/`acolyte`/`cult-fanatic`. **L4 run 5**: `NPC 'AlphaWolf' uses stat block 'dire-wolf' (AC 14, 37 hp, CR 1)`; player hits logged `vs AC 13/14` | FIXED (L4) |
| G-06 | S2 | Quests | A quest layer: stages, prerequisites, reveal-on-unlock, rewards, giver/turn-in, journal text | `ObjectiveTracker` is flat; `hidden` objectives exist but there is **no `reveal_objective` action**, so a chain cannot surface its next step | Objectives authored visible or never revealed; chain expressed with story variables | **PARTLY FIXED 2026-09-08**: `ObjectiveTracker::setHidden` + `reveal_objective` / `hide_objective` / `add_objective` actions in the scaffold executor (Ravenmere's main chain is now hidden-and-revealed step by step; side quests revealed by their giver's node). A real `Quest` type (stages/rewards/giver) is still open. **L4 2026-09-08 run 4**: `Trigger 'reveal_main_2' fired (when: objective_complete)` → `Objective 'main_2' revealed` in the shipped build after the Reeve's accept node | PARTLY FIXED (reveal L4) |
| G-07 | S2 | Triggers | Conditions on triggers (fire only if `priest_slain`) | Trigger `when` matches event payload keys only; no story-variable condition | The relic pickup trigger fires even if the boss lives | `when.condition` (same schema as dialogue choice conditions) | OPEN 2026-09-08 |
| G-08 | S2 | Screens | Inventory, character sheet, journal/quest log, party, spellbook, level-up choice | `ScreenState::Inventory` exists but nothing opens it; `CharacterSheet`/`CampaignJournal` are API-only; level-up is a log line + event | None | Data-driven screens on `UISystem` (needs a scrollable container widget) | OPEN 2026-09-08 |
| G-09 | S2 | Economy | Buy/sell with a merchant, currency check in dialogue | `CurrencySystem` library only; `give_item gold_crown` is the whole economy | Selby's shop is dialogue flavour | `shop` dialogue action / merchant screen + `has_item`/`gte currency` choice conditions | OPEN 2026-09-08 |
| G-10 | S3 | Pipeline | `phyxel.exe --project <dir>` should bind the project's port | `.phyxel/config.json` says `apiPort: 8104`; `engine.json` (written by `create_project.py`) has **no** `api_port`, so the editor bound **8090** (`EngineAPIServer` log line) — collides with any other session | Pass `--port 8104` explicitly | `create_project.py` must write `api_port` into `engine.json` (or the engine must read `.phyxel/config.json`) | OPEN 2026-09-08 |
| G-11 | S3 | Pipeline | `phyxel link --genre rpg` on a freshly scaffolded project should seed the RPG milestones | `create_project.py` links WITHOUT a genre, and a later `phyxel link --genre` **keeps the existing** `production.json`/`GAMEPLAN.md` — the genre is silently not applied | Deleted both files and re-linked | `create_project.py --genre` passthrough, and `link --genre` should merge new milestones into an existing tracker | OPEN 2026-09-08 |
| G-12 | S3 | Perf | Menu scene should be smooth | Release editor on the 3×3-chunk title world with a `cameraPath` orbit logged sustained **~177 ms frames** ("STUTTER DETECTED") right after load | — | Investigate (menuWorld orbit? first-frame shader/pipeline warmup? water GPU backend?) | OPEN 2026-09-08 |
| G-13 | S2 | Editor parity | Editor should execute the same trigger/dialogue actions as the shipped game | Editor executor lacks `save_game`, `long_rest`, `give_item`, `remove_item`, `join_party`, `start_combat`; shipped game lacks nothing the editor has except editor-only `show_victory` warn | Test the RPG loop in the packaged `--test` build, not the editor | Move the executor into `GameShell` (the long-standing "engine-side shell" item) | OPEN 2026-09-08 |
| G-14 | S3 | Docs | `resources/rpg/` cited in CLAUDE.md / README §10 | Does not exist; data lives in `resources/{classes,races,spells,monsters,loot_tables,rpg_items,factions,dialogues}` | — | Fix the docs | OPEN 2026-09-08 |
| G-15 | S1 | Editor stability | Clearing a strip of generated forest to make room for an authored shrine | `clear_region` (-50..-42, 17..40, 10..20 — 2,376 voxels of tree canopy/trunks) **killed the Release editor silently**: log ends at `Job 1 started: clear_region`, no exception line, process gone | Relaunched; avoid clear_region on flora; pick shrine site elsewhere | Repro + fix (likely template-owned voxels / flora instances removed under the job thread); the job system should catch and surface the failure | OPEN 2026-09-08 |
| G-16 | S3 | Settlement gen | Honest resident count from `build_settlement` | Result reported `residents: planned 14, spawned 0` with 14× `NPC 'res_…' already exists` — the residents WERE spawned earlier in the same build (the building phase), so the final phase double-spawns and reports a false failure | Ignore the number; `list_npcs` shows 20 NPCs | Spawn residents once; report the true count | OPEN 2026-09-08 |
| G-17 | S2 | Settlement gen / NPC grounding | Residents + authored NPCs standing on the ground | `list_npcs` right after the build: `res_bakery_-38_21` at y=29.3, `res_tavern_39_16` y=21.4, `res_hall_house_44_13` y=21.7, `res_croft_31_17` y=20 — on ROOFS; authored Hobb at placeholder (8,18,8) ended at y=27.7 (pushed to a roof by the spawn gate?) | Author NPC positions from measured furniture/door positions, outdoors where possible | Resident spawn should ground to the building's FLOOR (interior) or the door; SpawnGate should resolve to the nearest walkable cell, not the column top | OPEN 2026-09-08 |
| G-18 | S1 | Perf | Playable frame rate in the generated town on the min-spec laptop (RTX 1000 Ada) | Release editor: **2 FPS** from a high free camera (GPU: Foliage 354 ms, Static Geometry 122 ms), **10 FPS** at street level (Static Geometry 73 ms, 13 draw calls). GI (M5 probe field) is default OFF and adds only 21.6 ms when on — the static pass is the cost. Menu scene (3×3 flat + 4 fills) already ran ~177 ms frames | none | Profile the static voxel fragment path on this GPU class (unified lighting merge is the suspect: shadow cascades × smooth lighting); the game needs a `perf_target` on THIS class of GPU | OPEN 2026-09-08 |
| G-19 | S2 | Settlement gen | Civic/religious buildings a story needs: shrine/temple, town hall, jail/stocks | `room_program.json` has `town_hall` but the **town tier never picks it** (weights: hall_house/croft/tavern/blacksmith/general_store/bakery/apothecary/butcher); no shrine/temple/chapel typology at all; no stocks/gallows props | Reeve on the street in front of a hall_house; shrine = authored fills west of the street; stocks = a Wood fill | Add `town_hall` + a `chapel`/`shrine` typology to the town tier (and a `public` civic slot the story can address by role) | OPEN 2026-09-08 |
| G-20 | S3 | Settlement gen | Front doors face the main street | The Raven's Rest (`house_4`, plot 3) hangs its sign at (-25,19,-6): the door is on the SOUTH face, away from the main street at z=12; the tavern at plot 10 signs at its north face too | none needed for play | Orient the entry face toward the plot's street frontage | OPEN 2026-09-08 |
| G-22 | S1 | Settlement gen → shipped game | The generated town's 14 scheduled residents should ship with the town (rpg milestone `living_world`) | NPCs are not part of the world DB: after `save_world` + a scene reload only the 6 game.json NPCs exist (`Game definition loaded … npcs=6`); the residents and their schedules are gone | Authored 6 ambient `wander` townsfolk with one line each in game.json | Persist generator residents (name/role/schedule/home) into the world DB or export them into the project's game.json as a `residents` block the loader spawns | OPEN 2026-09-08 |
| G-23 | S3 | Pipeline | Building the game project should be quick | `create_project.py` emits a CMake that `add_subdirectory`s the WHOLE engine (`phyxel_build`, EXCLUDE_FROM_ALL) — a fresh project compiles phyxel_core from scratch in its own tree instead of linking the engine's already-built `build/engine/Release/phyxel_core.lib` | Background build; wait | Link the prebuilt engine lib (or an installed SDK) by default; rebuild the engine only on request | OPEN 2026-09-08 |
| G-24 | S2 | Dialogue | Choice visibility needs compound conditions ("tracks found AND not yet reported") | `DialogueChoice.condition` is ONE `{variable, op}`; no AND/OR, no `has_item`, no currency, no party-member test | Every used branch retires itself by flipping the variable it is gated on (`tracks_found`→false after telling the Reeve, `relic_taken`→false after hand-over…) — brittle and leaks into other gates (Oswin's join had to move to `barrow_open`) | `condition: {all:[…]} / {any:[…]}` + `has_item`/`gold_gte`/`party_has` predicates | OPEN 2026-09-08 |
| G-25 | S3 | Production tracker | `win_condition` static check should find the real terminal | It reported `terminal trigger action 'transition_scene' wired` (a scene door) although the real win is `show_victory` on `objective_complete main_6` — the validator takes the first terminal-looking action it meets | — | Prefer `show_victory`/`show_credits`/`quit_game`; report scene doors separately | OPEN 2026-09-08 |
| G-26 | S1 | Story state | `globalStory.world.variables` (initial story variables) and `globalStory.arcs` should be live in a shipped multi-scene game | `SceneDefinition` parses `globalStory` into the manifest and **nothing consumes it** (no reference in `engine/src`, `editor/src`, or the scaffold). Combined with fail-closed conditions, every `equals:false` gate on an unset variable hid its choice: the Reeve's only visible option was "Nothing for now" (probe run 1, `Ended conversation` 1.3 s after start) | Gate ONLY on variables gameplay sets (`equals:true`), retire branches by flipping to false; arcs are decorative | **PARTLY FIXED 2026-09-08**: the scaffold applies `globalStory.world.variables` once after `loadManifest` (logs `globalStory: N initial story variable(s) applied`). Arcs still unconsumed; the editor host still ignores the block. **L4 2026-09-08 run 3/4**: `globalStory: 17 initial story variable(s) applied` at boot | PARTLY FIXED (vars L4) |
| G-27 | S2 | Input / test API | A harness (and a player on keyboard) must be able to leave the pause menu; Enter must not double as "interact" | Injected `Escape` never closes the shipped pause menu (300 presses, `screen` stayed `paused`; a `ui/click` on Resume works), and `Enter` is both "advance dialogue" and "interact", so the Enter that ends a conversation re-opens it 20 ms later (log: `Ended conversation` → `Started conversation` on the next key) | Probe clicks Resume; presses exactly one closing Enter; never Escape | Route injected keys through the same edge handler the pause menu uses; separate the interact key from the dialogue-advance key (or consume the key on conversation end) | OPEN 2026-09-08 |
| G-28 | S3 | Test API | Party membership observable in the shipped build | `/api/rpg/party` → `unknown action: party` in `GameApiService` (editor-only route) | Infer from combat turn order | Add `party` to the standalone rpg actions | OPEN 2026-09-08 |
| G-29 | S1 | Input | Dialogue keys must fire once per press | The scaffold polled `isKeyPressed` (level-triggered) for E / Enter / 1-4 with no edge detection: one 100 ms Enter advanced TWO nodes — Bram's `join_party` action ran twice (`'Bram' joined the party` ×2, 116 ms apart) and the Enter that ended a conversation re-opened it | — | **FIXED in `create_project.py` 2026-09-08** (edge detectors for E/Enter/1-4). **L4 run 5**: Bram's `joined the party` logged ONCE | FIXED (L4) |
| G-30 | S2 | Progression | A pre-made cleric should have cleric stats | `/api/rpg/sheet` after boot: every ability 10, AC 10 (no standard array / point buy / racial bonus applied) → save DC 10, +0 to hit, 100 HP flat | — | `progression` should apply a class standard array (or `abilities` block) + racial bonuses; HP from hit die | OPEN 2026-09-08 |
| G-31 | S1 | Combat rules | A fleeing enemy must eventually LEAVE the encounter | `combat_ai.flee_below_hp` works (alpha wolf `FLEES from 'player' (hp 22%)`) but it flees every turn forever: 17+ rounds, the wolf 21 u away, no way to end the encounter (no "escaped" state, no auto-end while a hostile is technically alive, WASD dead so the player cannot chase, `combat/end` is API-only). Run 2 stalled here | Probe killed | **FIXED in the engine 2026-09-08** (`CombatAISystem`): after 2 consecutive flee turns and ≥45 ft from its foe the combatant ESCAPES (`removeCombatant`, deferred to `finishTurn` so the initiative index stays valid; auto-ends the encounter); the scaffold emits `combat_victory {reason:"enemies_escaped"}` on the combat exit edge when no kill ended it. No XP for escapees. **L4 run 5**: dire wolf `FLEES` ×3 then `ESCAPES the encounter (fled 3 turns …)`, `combat/state` → `in_combat:false` after 9 rounds | FIXED (L4); unit tests `CombatAISystemEscapeTest` 4/4 (`FirstFleeTurnDoesNotEscapeEvenWhenFar`, `RepeatedFleeingFarAwayEndsTheEncounter`, `NoMoraleBreakNoEscape`, `StalledBodyStillEndsTurn`) — the first draft escaped inside ONE turn (streak counted decisions, not turns); the test caught it |
| G-32 | S2 | Combat rules | Turn-based fights need 5e ranged/spell options for the probe and the AI to close distance | The API fight loop only had melee (`player_attack`) — vs a kiting enemy the player wasted 11 of 17 turns walking | Probe now casts `sacred_flame` when out of melee reach | (design note; the human has the spellbar) | NOTE |
| G-33 | S1 | Test API / input | Injected keys must release on schedule everywhere | `InputManager::tickInjection` (ages the `hold`) is called ONLY from `processInput`, and the shipped shell calls `processInput` only while playing-and-not-in-dialogue. In a dialogue, on the pause menu or a menu scene an injected key is **immortal**: run 3's second "1" never produced a new edge (conversation parked on the `children` node forever), and this is also WHY injected Escape could not close the pause menu (G-27) — the edge detector saw Escape held. The old level-triggered dialogue code only "worked" because the stuck key re-fired every frame (the double join of G-29) | — | **FIXED 2026-09-08**: `tickInjection` made public; the scaffold ages injections every frame it does not run `processInput`. Editor host checked: its `processInput` is unconditional (not affected). **L4 run 5**: every dialogue in town completed through the harness | FIXED (L4) |
| G-34 | S2 | Loader | `"invulnerable": true` on a quest-giver must hold | `GameDefinitionLoader::loadNPCs` applied `invulnerable` ONLY inside the `health`/`maxHealth` block — a plain `"invulnerable": true` NPC (every Ravenmere quest-giver) was killable | — | **FIXED 2026-09-08**: applied independently of the health keys | FIXED-PENDING-L4 |
| G-35 | S3 | Dialogue / harness | A harness (and the docs) must know the dialogue state machine | `DialogueSystem` types text out (`Typing` → auto or Enter/E skip → `ChoiceSelection` / `WaitingForInput`); a digit pressed while typing is IGNORED, Enter in ChoiceSelection is a no-op. Run 4's "1" landed during typing and the conversation parked. Not documented in GameCreationGuide; only the level-triggered key repeat had hidden it | Probe: Enter (skip) before every digit; Enter+Enter to end; +1 pair per linear hop | Document; consider accepting a digit during Typing as "skip + select" (BG3 does) | OPEN 2026-09-08 |
| G-36 | S2 | Scenes | Coming back through a door should put you at THAT door | Scene re-entry spawns the player at the scene's default `player.position` (run 5: back from the cellar → town spawn at the west end, not the tavern trapdoor; Hearthvale noted re-entry "inside the departure region" — behavior differs by path) | Doors are one-shot; players walk back | `transition_scene` action takes an optional `spawn: {x,y,z}` / `spawn_at: "<location id>"`, and the SceneManager re-entry data records the door used | OPEN 2026-09-08 |
| G-37 | S2 | Inventory | An RPG ships with a survival-style (finite) inventory, not the editor's creative hotbar | `/api/rpg/inventory` in the shipped build reports `"creative": true`; the HUD hotbar shows nine block stacks ×64 (magenta Default first) — the editor's block palette, not the cleric's belongings. `Inventory::setCreativeMode` exists but the scaffold never sets it from game.json | — | `game.json inventory: {mode: "survival"}` (default survival for rpg), start-with items from `progression`/`starting_inventory`, hotbar bound to item props not blocks | OPEN 2026-09-08 |
| G-38 | S1 | Lose condition | The player dying in combat must END the fight and show game-over (rpg `lose_condition` milestone) | Run 5: `npc_Wolf2 hit player for 10 damage — KILLED`, then the DEAD player kept taking turns (`player hit npc_Wolf2 — KILLED` 30 s later), `entity_health player → alive:false, health:0`, screen stayed `playing`, no `player_died` event in the log, no game-over screen | — | **FIXED 2026-09-08**: `ScreenState::GameOver` + `GameScreen::showGameOver()` + `resources/ui/game_over_screen.json` (Main Menu / Quit); the shell ends the encounter and shows it on player death (opt out: `lose.auto_game_over:false`), `show_game_over` trigger action, test API reports `game_over`; `PlayerTurnController` skips a dead player's turn. Retry-from-last-save button is a follow-up. **L4 run 9**: `npc_Wolf3 hit player … KILLED` → `Player died — game over` → `/api/screen/state` = `game_over` with `game_over:main` visible, `combat/state in_combat:false` (`rv_run9_gameover.log`, `rv_game_over_screen.png`) | FIXED (L4) |
| G-39 | S3 | Logging | `LOG_INFO("… {:.1f} …")` must not be possible to write | The Logger's `{}` takes no format spec; a spec prints verbatim and SHIFTS the following args — my own ESCAPE log line was the 5th instance of this bug in the codebase | Fixed my line | Make the Logger reject/strip format specs at compile time or support them | FIXED (mine) / OPEN (root) |
| G-40 | S3 | Triggers | A pure "observe this event" trigger (for a harness or analytics) | `TriggerSystem` rejects `then: []` (`Skipping invalid trigger: trigger needs a non-empty 'then' array`) — you must attach a dummy `set_story_variable` to watch an event's `fired` flag | Dummy flag actions | Allow an empty `then` (fired flag only) or add a `log`/`event` action | OPEN 2026-09-08 |
| G-41 | S2 | Party | A companion's death must mean something | Run 6: `npc_Wolf2 hit npc_Bram for 13 damage — KILLED` on the farm, then `Companion 'Bram' rejoined at the player's side` in the barrow — the shell respawns "living" members on every scene load but never marked him dead (`Party::setAlive` unused); kill XP was also granted for him | — | **FIXED 2026-09-08 (scaffold)**: death branch marks the party member dead (`companion_died` event, no XP), a `long_rest` revives the fallen (house rule for a short game) and heals the player; UI feedback (portrait/“fallen”) still open. L4 pending a companion death in a passing run (none died in runs 11–13) | FIXED (unit-level) |
| G-42 | S1 | Combat AI | An NPC turn must always END | Run 6 crypt fight: Wren's turn looped `decision: advance — OUT OF REACH: npc_Skeleton3 is 1u away vs 1u reach` ~25×/s for minutes (dist 1.41 u vs reach 1.52 u by ENTITY positions, yet `TurnActor::inReach` — which reads the BODY position — said no; the move to "just inside reach" completed instantly, so nothing was spent and the AI re-decided forever). Wren and Bram stood on the same spot (18.9,18.1 / 18.8,18.1) — companions respawn on top of each other | — | **FIXED 2026-09-08 (engine)**: `CombatAISystem` caps decisions per turn (12) and ends a stalled turn with a WARN + CombatLog `stalled` entry carrying body/entity/target positions and budget flags; unit test `StalledBodyStillEndsTurn` (guard fires: `turn STALLED after 12 decisions`); `TurnActor` also drops a move that makes no progress for 1.5 s. Root causes still open: body-vs-entity position drift in reach tests; respawned companions share one position. **L4 runs 11–13**: three crypt fights with the full party resolved (6–8 rounds), no stall | FIXED (L4 guard) |
| G-43 | S2 | Difficulty / pre-made | The authored cleric should survive a CR-appropriate fight | AC 10 (no armor, no shield — `progression` grants no equipment), +0 everywhere (G-30); a 3-wolf + leader pack killed the player in 2 of 3 runs even with a fighter companion and self-healing. Rats and skeletons were fine | Wolf pack reduced to 2 + leader | `progression.starting_equipment` (chain mail + shield for a cleric → AC 18) and class standard array; an encounter-budget check (CR vs party) in the production validator | OPEN 2026-09-08 |
| G-44 | S1 | Combat rules | The PLAYER's armor class must be their sheet's AC | Enemy attacks resolve against the pseudo-AC `8 + floor(HP%·6)` for the player too — run 10: wolves rolled `vs AC 13` at full HP, `vs AC 9`, then `vs AC 8` as the cleric bled (a death spiral: the hurt get easier to hit); the sheet (`/api/rpg/sheet armorClass 10`) was never consulted | — | **FIXED 2026-09-08**: `CombatAISystem::setACProvider` (the shell answers the player's sheet AC); `progression.abilities {str..cha}` + `progression.armor_class` author a real pre-made (Ravenmere: standard array, AC 18). **L4 run 11**: `Player sheet: AC 18, STR 14 … WIS 16`, rats rolled `vs AC 18`; the cleric finished the wolf fight at 100/100 (was dead in runs 5, 9, 10) | FIXED (L4) |
| G-45 | S2 | Combat rules | Companions fight with their weapons | Turn-based NPC damage is stat-block-or-`1d4`; a game.json `weapon` on an NPC is ignored by `CombatAISystem::resolveEnemyAttack` — Bram (fighter) punches wolves for 1d4 | — | **FIXED 2026-09-08**: `combat_ai[id].weapon` (rpg_items id → damage dice) / `damage_dice` / `attack_bonus` feed `CombatTactics` and `resolveEnemyAttack` when no stat block applies (Bram: mace +4, Wren: shortsword +4, Oswin: quarterstaff +2). **L4 run 11**: `npc_Bram hits npc_Wolf for 6 (1d6)` | FIXED (L4) |
| G-46 | S2 | Character movement | A 1-voxel step (an altar dais, a stair riser) should be walkable without a jump | Run 11: the cleric stood at the dais edge (z 11.3, dais at z ≤ 11) for 90 steering iterations — the 1 m step onto the `Stone` dais was never climbed; the relic region ended at the dais | Region extended to the floor in front of the altar | Step-up (auto-climb ≤ 1 voxel) in the kinematic character, or level design guidance + a validator warning for reach-required regions on raised geometry | OPEN 2026-09-08 |
| G-47 | S2 | Test API | A harness must SEE the dialogue it drives | The shipped test API had no dialogue state; choices were picked by blind index and run 12 chose "I found tracks" instead of "I have his relic" at the finale (an earlier region miss had shifted the visible list) — the win never fired | — | **FIXED 2026-09-08**: `POST /api/rpg/dialogue_state` (state, speaker, node, text, visible choices with index/target/skill_check) via a `GameShell::apiDialogueSystem` hook; the probe now picks by text and records what it saw. **L4 run 13**: every conversation driven by visible text (`[Persuasion DC 12] Release the poacher W…` seen and picked), finale chose `The Hollow Priest is dead. I have his re…` → `Take it` → `Trigger 'win' fired` | FIXED (L4) |
| G-48 | S1 | Scenes / pipeline | A pre-baked scene world must LOAD in the shipped game | `SceneManager` loads `worlds/<scene>.db` only when the scene definition has **no** inline `world` key; with one it regenerates the terrain from the definition every launch and never opens the saved chunks or placed objects. Ravenmere's town scene kept its `world` block after the bake, so **every standalone run (1–13) played on a flat field with NPCs standing on grass — no town, no tavern, no walls.** I saw the town only in the editor and reported the shipped game as having it; the user's first play found the field. The docs only say a `world` key "generates terrain on first load", not that it overrides a saved DB, and nothing warns when a DB with chunks is ignored. The standalone also saves its regenerated flat chunks back into that DB on scene exit | `world` moved to `_world_baked` in the town scene; town REBUILT with the settlement generator (same seed, same layout) because the 10:53 save had overwritten the baked chunks with the regenerated field; verified by leaving/re-entering the scene in the editor AND in the shipped exe (`Loading 27 chunks from database`, `rv_town_shipped_after_fix.png`) | Prefer the DB when it has chunks (or warn loudly on the conflict); `create_project.py`/`package_game.py` should strip `world` from scenes whose DB is baked; doc the rule | FIXED (authoring) / OPEN (engine) |
| G-49 | S3 | Rendering / shipped shell | First seconds after a scene load should not be black | Shipped build: a screenshot ~3 s after entering the town scene was almost black (only nameplates/faint shapes); at ~25 s the same street was fully lit. Likely lighting settling after a DB load | Wait | Investigate skylight/flood settle on `loadAllChunksFromDatabase` in the standalone; keep the loading screen up until lighting is ready | OPEN 2026-09-08 |
| G-50 | S1 | UI | Menus and HUD must survive a window resize (and different resolutions) | Playtest 2026-09-08: after resizing the window every UI element is in the wrong place. Menu/HUD JSON uses absolute `position: [x,y]` in a 1280×720 authoring space (`freeLayout` panels); `UISystem` resolves panel anchors against the live `screenWidth_/screenHeight_` but the widget positions inside are raw pixels — no reference-resolution canvas scale, no re-layout on resize. The engine.json window is 1280×720, so the layouts only line up at exactly that size | None (play at 1280×720) | `UISystem`: treat authored positions as a 1280×720 virtual canvas scaled uniformly to the window (letterbox or anchor-aware), re-run layout on the framebuffer-resize callback; long term, author screens with anchors + offsets instead of absolute pixels | OPEN 2026-09-08 |
| G-51 | S1 | Wayfinding (design decision) | The user's direction (2026-09-08 playtest): NO quest markers/arrows — exploration should be the game — but a **map / minimap**, a **compass** so directions mean something, and a **dialogue log** so what an NPC said ("the shrine on the green south of the hall", "east on the road") can be re-read | None of the three exists in a shipped game: WorldForge has an ASCII minimap route (`/api/worldforge/map`) but no HUD minimap widget; no compass; DialogueSystem keeps no transcript and CampaignJournal is editor-API-only | Objective text carries directions | HUD minimap (chunk-derived top-down + NPC/player dots), a compass strip driven by camera yaw, and a dialogue log screen fed by `dialogue_node_reached` (journal of who said what) | OPEN 2026-09-08 |
| G-52 | S1 | Perf (shipped) | Playable frame rate in the town on the min-spec laptop | User: "fps on the game is pretty shitty now" (with the real town loaded). Field-only runs measured ~50 FPS in the standalone; the town view is being measured per step in probe run 14 | — | Profile the shipped build in the town (static geometry pass, foliage, NPC count), then the same levers as G-18 | OPEN 2026-09-08 |
| G-53 | S1 | Navigation (shipped) | The nav grid must describe the CURRENT scene | Run 15: `Built NavGrid: XZ [-32,-32] to [63,63], 9216 cells (9216 walkable)` — built ONCE while the menu scene's 3×3 title world was loaded, never rebuilt for the town: the spawn at x=-38 is "Cell not in grid", every cell is walkable, so `navgrid_path` routes straight through the hall and the probe (and any wandering/following NPC) walks into walls. The residents' `findPath failed: goalCell=NULL` / `A* exhausted` warnings in earlier runs are the same bug | Straight-line steering | **FIXED 2026-09-08 (scaffold)**: `onSceneReady` rebuilds the NavGrid for every world scene (run 16: `Built NavGrid: XZ [-64,-64] to [95,95], 25600 cells` after the town loaded; the path around the hall to Maera worked) | FIXED (L4) |
| G-54 | S1 | Navigation | NPCs (and a path-following harness) must be able to path THROUGH a doorway | `NavGrid` marks any cell with a neighbour more than 2 blocks higher as `nearWall` and excludes it from links; a 1-cell door gap between two 5-high walls is therefore impassable, and a whole building interior is an island (`navgrid_path` into the Raven's Rest: `nodesExpanded 2`, not found; to a cell beside its wall: 1091 nodes, not found). Companions cannot follow you indoors by path, residents cannot reach their homes, and the town's `STUCK (replan)` warnings come from this | Probe falls back to straight steering indoors | Make `nearWall` a cost, not a hard exclusion (or exempt cells whose blocked neighbours are on opposite sides = a corridor), and treat authored door frames as passable | FIXED in the engine (2026-09-09, increment 4): `NavGraph` micro mode (sub-cube occupancy, micro feet/headroom, build-time swept edges with lateral slack, step-straddle rule), micro-precise non-voxel obstacle overlay, static templates no longer rasterized. Unit: `NavGraphMicroTest` (13) + `NavObstacleTest`. L4 on the old town DB: interior samples reachable 0/146 → 92/146, street→Bram found. Remaining unreachable rooms are generator defects (G-64 doors, G-55 furnishing), re-measured after the pristine regen. |
| G-55 | S2 | Settlement gen (furnishing) | The way in from the front door must be clear | Raven's Rest (house_4): the entry door on the south face at x≈−25 opens onto a bar stool at (−24,−5) with the bar counter at (−23,−5): the doorway is blocked one metre in; a player (and the probe, run 19) walks into the stool. Furnishing does not reserve a clear lane from the door | Probe sidesteps west | The furniture placer should reserve a door lane (door cell + 2 cells inward, 1 wide) and `RealizedWorldValidator` should flag furniture within it | PARTLY FIXED (2026-09-09): `planYardProps` now keeps `kDoorApproachCubes = 2` (the gate corridor) between a rear-wall prop and the building (`YardPropsTest.RearWallPropsKeepTheDoorApproachClear`); the interior stool-in-doorway case is still open (furnishing rule, increment 2). |
| G-56 | S3 | Triggers (authoring) | Region boxes should be validated | A region authored `from x:-24 to x:-24` has zero width, so no player position ever satisfies it (run 21's trapdoor). Nothing warns; the trigger silently never fires | Regions ≥ 1.5 u on every axis | `TriggerSystem` validation: warn on any region axis thinner than a character (0.85 u) | OPEN 2026-09-08 |
| G-57 | S2 | Structure gen / collision | Walls with shuttered windows must still be walls | Run 22: the character entered the Raven's Rest by its door at (−25,−5.4) and 47 s later stood OUTSIDE the west wall at x=−30.3 without using a door — the west face has `window … infill: shuttered` openings at z −4/0/5/9 and the character walked through one. Shutters have no collision (or the opening reaches the floor) | Cast placed so no route passes a window | Give window infill (shuttered/glass) collision, or keep window sills ≥ 1 block above the floor with a solid lintel test in the realizer validator | OPEN 2026-09-08 |
| G-58 | S2 | Interaction | Choosing WHOM to talk to when two NPCs are near | Interact picks the nearest NPC within 3 u; a following companion (deadzone 3 u) is often nearer than the shopkeeper you face, so E talks to Bram instead of Hobb. The facing cone was removed for the +Z bug; no cursor/aim targeting exists | Probe walks right up to the bar and retries | Aim-based or cursor-hover interaction target (BG-style click), or prefer non-party NPCs when a companion is within the radius | OPEN 2026-09-08 |
| G-59 | S2 | Settlement gen (layout) | Adjacent buildings on a street block need alleys, or the "behind the house" space is unreachable | Run 24/25: the tavern (house_18, x -29..-21) has the croft (house_16) ABUTTING its west wall (0 gap) and the hall (house_20) 1 m off its east wall. Nothing behind the tavern is reachable except by walking round the whole block (~50 m) — the probe only got there by sliding through a shuttered window (G-57). A lot-side gap ≥ 1.5 m (or an explicit alley per N lots) belongs in the settlement lot planner. | OPEN |
| G-60 | S1 | Navigation | The nav grid must describe the GROUND, not the topmost voxel in a column | `NavGrid::findSurfaceY` scans from y=128 DOWN and returns the first solid voxel, so anything overhead becomes the "floor": the tavern doorstep column x=-25 reads `surfaceY=21` (the lintel/porch canopy) while its neighbours read 16, which marks the whole doorstep pocket `nearWall`, and a path started on it expands 2 nodes and dies (`navgrid_path` from the door to the street: `found:false, nodesExpanded:2`, live in run 25; `tools`: `navgrid_cell` grid dump z -6..-11 in the session log). Roof cells (y=23) are likewise "walkable". Fix: choose the walkable surface nearest a reference height (terrain height / the requesting entity's feet) with MIN_HEADROOM above it, or keep several layers per column (real 2.5D). This is a sibling of G-54 and probably the larger cause of unpathable doorways. 2026-09-10: the shipped `navgrid_path` command is now answered by the sub-cube NavGraph (`legacy: true, backed_by: navgraph`); `navgrid_cell` and the NPC nearWall relocation (cube mode only) are the last consumers. L4 run 46 (12:05): `navgrid_path` street (-36,15) -> tavern storeroom (-24.5,8.5) on the live shipped build = `found: true, 7 waypoints, legacy: true, backed_by: navgraph` (the original G-60 red was `found:false` for every interior). | RETIRED as an oracle (L4) |
| G-61 | S1 | Settlement gen (validation) | The walkability gate must RUN in the settlement build, on the realized world | `core/SettlementWalkability` (the L3 walkable-by-construction gate, 2026-07-28) has exactly one caller: its own unit test. `build_settlement` runs geometry checks only, so the Ravenmere town (medieval/town seed 7, furnished + item-dressed) was never gated, and the gate's composition excludes furniture/items so it could not have seen the doorway stool (G-55) anyway. User direction 2026-09-08: walkability of town/structures/doorways is the settlement-forge's job, and a playtest that finds a broken thing must feed back into the generator — plan: `docs/structure-generation/WalkabilityGateAndPlaytestLoop.md`. | OPEN (plan written) |
| G-62 | S2 | World hygiene (regen procedure) | Regenerating a settlement must start from a pristine scene DB | `placed_objects` on the rebuilt town: 334 objects, 45 duplicate (stem, position) groups — garden beds and woodpiles from the pre-G-48 build survived the G-48 regeneration and sit exactly under the new ones (e.g. two garden beds at (−25,17,−8), right in front of the tavern door). Duplicates double obstacle volume and item counts. The loop in `WalkabilityGateAndPlaytestLoop.md` §3 step 4 must delete the DB and regenerate terrain before `settlement/build`. | FIXED by regeneration (2026-09-09): town rebuilt from a pristine DB (temporary inline Flat world, seed 7, then `settlement/build` seed 7, 101 s, 24/24 units, 12 buildings, 269 objects; the only same-cell duplicates are stacked item props). Procedure hazards logged as G-66. |
| G-63 | S2 | Navigation (shipped) | The shipped game must feed NPC navigation the same non-voxel obstacles the editor does | `setNavObstacleProvider` is installed only by the editor (`Application.cpp`); `GameShell`/the scaffold install none, so in a packaged game NPC paths ignore kinematic item props and activated dynamic furniture entirely (treadmill class of bug, editor-only fix from 2026-07). Wire the same provider (item props via `ItemPropManager::worldAabb`, active dynamic furniture) in `GameShell`. | OPEN |
| G-64 | S1 | Settlement gen (layout) | A building's entrance must face its street | `generateRoomLayoutFromProgram`: typologies without a grounded entrance wall (tavern, town_hall, general_store, blacksmith, bakery, apothecary, butcher, tenement, tower_house, manor_hall — i.e. every commercial building) took the legacy perimeter-first pick on room 0 and IGNORED the settlement's `front` hint, so the Raven's Rest's only door opened onto the rear toft (column dump 2026-09-09: no opening anywhere on the street-side wall z=10) while the settlement paved its spur to the front wall's midpoint. Fix (2026-09-09): honour `front` — the room spanning the front wall's midpoint gets the door, else any room on that wall; legacy pick only without a hint. Test `OpeningsLayoutTest.FrontHintPlacesTheUngroundedEntranceOnTheStreetWall` (6 orientations, red on the old code). | FIXED (unit + L4 2026-09-09): in the regenerated town the bakery, butcher and hall doors sit on their street-facing walls (door scan evidence). Side effect: the tavern lot now fails the hearth gate (G-69). |
| G-65 | S1 | Scenes / items | Item props must be re-instantiated when a scene's world loads | `SceneManager` reloads placed objects from the scene DB on transition but nothing calls `ItemPropManager::rebuildFromPlacedObjects` (the editor does it only at project load, `Application.cpp` ~2055; `GameShell` never). So after entering the town scene the tavern's tankards, goblets, plates and the hanging trade sign exist as registry records with NO body and NO render group — the "items everywhere" dressing is invisible in a scene-loaded world, editor and shipped alike. Found via the nav overlay (the sign fed only its integer registry bbox because the prop manager did not know it). Fix: give the scene subsystems the ItemPropManager and rebuild props after `loadFromDb` in both hosts. ROOT CAUSE (2026-09-10, found by the transition markers): the shipped scaffold never constructed an ObjectTemplateManager or a PlacedObjectManager, so `GameDefinitionLoader` (which guards every template/item/marker placement on `subsystems.placedObjectManager`) silently skipped them in EVERY standalone build - only the editor had the managers. Scaffold now owns both (`templates_`, `placedObjects_`) and hands them to the loader. | SCAFFOLD FIXED (item re-instantiation on scene reload still to verify) |
| G-66 | S2 | Scenes / editor | Loading a scene whose world DB is EMPTY must not kill the host | Pristine-regeneration attempt 2026-09-09: with `worlds/town.db` deleted, the editor created an empty DB, loaded the town scene with 0 chunks (`Occupancy grid rebuilt from 0 chunks`, `No terrain occupancy grids registered … characters will fall through all terrain`) and the process vanished within a second, no crash line in the log. A scene with no chunks needs an honest fallback (refuse the transition, or hold the player) — and the regen loop needs a first-class "regenerate this scene's world from its recipe" path instead of the temporary inline-`world` trick used here. | OPEN |
| G-67 | S3 | Structure gen (signage) | A flush trade sign beside the door must hang above head height | Regen build log 2026-09-09: `place_signage: hung prop_sign_butcher (flush) over entry door (clearance 9 micro, above lintel -9 micro)` (same for the bakery): the flush form allows a board bottom at floor + 9 micro (1.0 m) when it slides beside the door, so the sign hangs at chest height on the facade. `planSignMount` should keep `kMinClearMicro` (19 = 2.13 m) for every flush board, not only the over-the-door one. | OPEN |
| G-68 | S1 | Settlement gen (thresholds) | A door threshold must be climbable: ≤ the character's 4-micro auto step, or the spur must stoop up to it | Regen town, hall (house_5): door cells at (−15,10) and (−15,3) stand at feet 17.56 (slab top) while the paved doorstep is 17.00 — a 5-micro (0.56 m) riser at the threshold, above `m_maxStepHeight` = 4/9 m, so neither NPCs nor the player can step in without jumping (column dump `docs/evidence/ravenmere/inc4_regen_town_door_scan.txt`). The tavern's 3-micro slab was fine. Fix: the paving spur / doorstep must be stamped up to threshold − ≤4 micro (a stoop), or the shell's ground-floor slab clamped to ≤ 4 micro above grade. | FIXED (L4 2026-09-09): 5/14 front doors stooped in the regenerated town; the hall's doorstep now links to its door (edge both ways) — `docs/evidence/ravenmere/inc4_regen5_approach_interiors_110of146.txt`. |
| G-69 | S1 | Structure gen (hearth) | The hearth must be SITED where its stack clears the upper rooms, not refused after the fact | Regen 2026-09-09: `lot 3 (tavern) refused: realize failed: chimney from the fireplace in 'kitchen' would rise through the middle of room 'chamber_1' on story 1 — re-site the hearth` — twice (re-roll too), so the Raven's Rest lot is EMPTY in the regenerated town. Moving the entrance to the street (G-64) moved the stair, which moved the upper chambers under the kitchen flue. The gate is right to refuse; `HearthForge::siteHearths` must include the upper-story stack check among its candidates (or re-site on refusal) so the tavern builds. Until then the shipped game has no hub building. | FIXED (2026-09-09, L4): three generator changes — vented pieces prefer EXTERIOR walls and may take window cells (the furnish pass treated window walls like door walls, so every kitchen fireplace backed onto the interior partition), the chimney breast drops the window it displaces, the upper-room stack rule lives in one place (`HearthForge::stackCrossesUpperRoomMiddle`) with a one-cube breast-band tolerance, and `siteAllStories` re-sites a violating hearth (never silently drops it). `HearthUpperRoomSitingTest` sweeps 384 two-story taverns (two wall thicknesses): 0 violations, 0 windows on hearth walls, all realize. Regenerated town: the Raven's Rest lot builds again (house_4, 16/21 interior samples reachable), 13 buildings, interiors 99/146. Evidence `docs/evidence/ravenmere/inc4_regen2_*`. |
| G-70 | S3 | Settlement gen (residents) | The resident pass must not re-spawn what the build already spawned | Regen build log: residents were spawned during the build (`Spawned procedural NPC 'res_bakery_-33_17'` 10:29:30) and a second pass at the end logs `NPC … already exists` → `resident spawn FAILED` ×13 → `residents: 0/13 spawned`, a misleading zero. Fix (2026-09-09): the settlement's final resident pass ADOPTS an NPC that already exists under the plan's deterministic name (refreshing its schedule) and reports `N spawned + M adopted of P planned (F failed)`; the JSON gains `adopted`/`failed`. Built; verify on the next regen log. | BUILT |
| G-71 | S2 | Settlement gen (furnishing) | Nothing may stand in the cell directly inside an exterior door — including the building's own partitions | Regen town hall (house_5): the cell behind each door, (−15,9) and (−15,4), has no ground surface — partial cubes from y17 to y20 (furniture or a partition) fill the interior threshold — so even with a climbable riser the door leads nowhere (door scan evidence as G-68). The door-approach band rule (increment 2) must apply INSIDE as well as outside. | FIXED (L4 2026-09-09): the hall's passage cell is standable and linked; the hall is enterable (interiors 110/146 overall). |
| G-72 | S1 | Settlement gen (fences/spurs) | The parcel fence must gap at the ACTUAL door, and the spur/gate must meet it | Regen town, hall (house_6): the picket fence runs along the frontage z=11 hugging the facade, gapped only at the paved spur column x=−11/−12 — the front wall's MIDPOINT, which is where the settlement assumes the door is — while the hall_house's cross-passage door is at x=−15. Micro scan: Log slats + two rails (y 17.11–17.78) across the doorstep cell; the doorstep→door edge is absent for exactly that reason. The gate opens onto a wall and the door is fenced off. Fix: (now) the door-approach unit clears every non-paving micro in the two cells outside each front door before stooping; (right) plan spur + gate from the realized entrance (autofill is deterministic, so the settlement can resolve it before paving). | MITIGATED (2026-09-09): the door-approach unit cleared 4,088 micro of fence/spill from the 14 front doors' approach bands; the hall door now has both crossings. The gate/spur still sit at the front-wall midpoint — the planning fix (resolve the entrance before paving) remains OPEN. |
| G-73 | S2 | Furnishing (tavern) | A bar needs a service aisle behind it | Regen tavern (house_4): the cell behind the bar, (−22,−3), where the innkeeper stands, has NO surface — the back bar/shelving against the east wall fills the 1-cube gap between the wall and the bar, so nobody can stand behind the bar. Hobb was moved to the bar's customer end (−24,18,−2) for the playthrough; the taproom recipe must reserve a ≥1-cube aisle behind the bar. | OPEN |
| G-74 | S2 | Typology (tavern) | The public room should meet the entrance | Regenerated Raven's Rest: the street door (north wall, G-64) opens into the SERVICE room; the taproom with the bar is at the far (south) end behind the kitchen, because the tavern's rooms are laid along the long axis in recipe order (taproom, kitchen, service) without regard to which end the `front` hint put the door on. A customer walks through the store and kitchen to reach the bar. Fix: order the bay sequence from the entrance end (or mirror it when the front is on the far end). | OPEN |
| G-75 | S1 | Player movement (BG-like) | Click-to-move / walk-to: the player should be able to walk a nav path, not only steer with WASD | Runs 26–28 on the regenerated town: the world is now pathable (NavGraph street→shrine through the 1 m gap, street→tavern interior) and NPCs walk it exactly, but the player — and the harness driving the player with injected keys, camera-relative at a fixed yaw — drifts 1–2 m on long spans and wedges on fence lines and building corners (run 26 pinned on the tavern's fence line, run 27 between fence and wall, run 28 at the gap's south mouth). A Baldur's-Gate-like needs click-to-move (mouse ray → NavGraph path → the character follows it); the same `walk_to` becomes the harness's honest steering primitive and the standalone API should expose it (`/api/rpg/walk_to`). | ENGINE + SHELL (CombatUiBg3 increment 5, 2026-09-15): `Core::ClickToMove` walks the SAME NavGraph route the NPCs use through the same `ITurnActorBody` adapter the TurnActor drives in combat; outcomes are honest (`arrived` / `no_path` / `stalled` after 2 s without progress / `cancelled`); a `standoff` + `onArrive` walks to an NPC and interacts. The shell: turn-based rulesets are POINTER-driven — the cursor stays free during play, LMB is a pointer click (`ControlScheme::pointerClicks`, never a swing), a click on an NPC walks to it and talks, a click on the ground walks there (`ClickToMove::pickGround`, a voxel DDA from the cursor ray), WASD / dialogue / combat cancel a walk. API `walk_to` / `walk_status` / `pointer_click` (the same code path as a real click). `ClickToMoveTest.*` (7). L4 2026-09-15 (probe build, `rv_clicktomove_l4.log`): API walk 12 m → 4 cm off the goal; ground click → 1 cm; NPC click on Aldric → walked to standoff 2.4 m and opened his dialogue; W cancelled a 34 m walk at 9.9 m; an off-viewport click is refused; lint 0. Shots `CTM_*` in the probe tree. |
| G-76 | S1 | Character controller | The player must slide into a doorway, not stop dead on its jamb | Run 31: the player stood square in front of the tavern's open street door, aligned to 12 cm of the reveal's centre (`steer_stuck (−24.62, 11.33)`), and could not enter: the reveal is 0.78 m (grounded), the controller 0.52 m wide, and `resolveKinematicMovement` reverts a blocked axis move outright. NPCs walk the exact centre line so they never noticed. Fix (2026-09-09): a doorway funnel — when a move is blocked, try lateral shifts up to a quarter body width on the other axis and take the move from the first free one; solid walls still block. Test `CharacterDoorFunnelTest` (centred passes, ±12 cm off-centre passes, solid wall still blocks). Pending L4 (run 32). | FIXED (unit, 2026-09-09): `CharacterDoorFunnelTest` 3/3 — centred and ±12 cm off-centre walks pass a 0.78 m micro-built reveal, a solid micro wall still blocks (the fixture had to set the grid's cube and subcube bits: `queryAABB` descends only through set+subdivided levels). Shipped exe rebuilt; L4 = probe run 32. |
| G-77 | S1 | Structure gen + character controller | Doors must be as tall as the canon says and the player's collision box must be the model, not the model plus a third of a metre | Run 32: with the funnel in, the player still stopped 6 cm short of the tavern's door plane. `CharacterDoorFunnelTest.RealizerHeightDoorAdmitsTheCharacter` reproduced it headless: the realizer cut exterior doors 16 micro (1.78 m) clear because it painted the 2-micro lintel INSIDE the 2-cube opening, while the canon (`object_dimensions.json`, DimensionCanonTest) says door_interior clear_h = 2.03 m; and the controller box was 2.12 m (model 1.82 m + 0.3 m margin). Fixes (2026-09-09): exterior door lintels now sit in the wall above the opening (2.0 m clear; windows unchanged; a door reaching the wall top keeps its lintel inside), and the head clearance is 0.05 m (`kControllerHeadClearance`, goldens re-recorded with this reason). Test green on both. L4 (run 33, 14:52:09): the player walked the street door and reached the storeroom - the `to_cellar` floor trigger fired inside the tavern (`Trigger 'to_cellar' fired (when: entity_reached_region)`), the first time any run entered a generated building through its door; the hall door column reads 18 micro headroom (was 16). | FIXED |
| G-78 | S2 | Settlement gen (fences vs neighbours) | A residual gap between a parcel fence and the next building must be a real corridor or closed | Run 33: the NavGraph's only street->shrine route threads cell (-21,-5) between the stone house's east wall (x=-22, 21-32 Stone micros of plinth/quoin spill into the cell) and the picket fence's west end post (x=-20): the edge exists but with lateral slack 1 micro (0.11 m) - the mover must hit the cell centre to 11 cm. Run 32 got through by luck, run 33 slid along the fence to (-13.8,-4.6) and never reached Maera. MEASURED (editor occupancy dump, 2026-09-09 15:50): the tavern's east wall face is at x=-21.0 (a 0.667 m ashlar band in cube x=-22), the neighbouring parcel's picket line is at x=-20.0, so the alley the route uses is 1.0 m clear; at the tavern's south-east corner the dressed-stone QUOIN (realizer pass 4.5, 1/3-cube proud) narrows it to 0.67 m for one cell - the 0.11 m slack. Generation rule needed: a fence plane must not run within 2 cells of a neighbouring building's wall face (leave the margin unfenced or push the plane out), and quoins must not eat a corridor below its minimum - lot-permeability work, increment 3 / G-59. FIX (2026-09-09, first half): `fenceRunPinchesNeighbour` (FenceBuilder) - a parcel plane that would leave < 2 clear cells against a NEIGHBOUR's footprint over an overlapping span is dropped by the settlement fence unit (logged as `runs dropped (alley < 2 cells to a neighbour wall)`, JSON `fence_runs_dropped_for_alley`); N/E planes' 8 inner micro count as clear width. Red test `FenceCornerTest.RunsPinchingANeighbourWallAreDropped` (23/23 fence + settlement suites). The 2-cell minimum is `SettlementWalkability::kMinCorridorWidthCubes` (already the settlement's built corridor requirement and the fence stamper's gate width; grounded there in the agent box), now cited by the fence unit rather than restated. Regen #7 L4: 7 runs dropped, the alley's east side opened - but the dropped plane's neighbours still planted a corner post on the pinch line (0.67 m at the quoin, slack 1). Second half: `trimFenceRunAtDroppedCorners` pulls a surviving run back 2 cells from every dropped corner and posts its free end (test `SurvivingRunsPullBackFromADroppedCorner`, 24/24). Regen #8 L4 (16:25, editor `navgraph_column`): the corner post at (-20,-5) is gone (slack 4 all round), the quoin cell (-21,-5) is open to the east (+x slack 4), and the street->shrine route no longer touches the quoin - it runs down x=-19.5 and turns east at z=-2.5 (11 waypoints, tightest crossing slack 2 = 0.22 m). Interiors 110/146 unchanged. Quoins-into-corridors (a quoin proud into a corridor that has NO fence) not addressed - noted for the lot planner. Run 37 = the L4 walk. | FIXED (L4 graph); walk pending |
| G-79 | S2 | Navigation (movers) | Movers advance waypoints at 0.5 m while the graph knows some crossings tolerate 0.11 m | `ScheduledBehavior` advances a waypoint within 0.5 m and the harness used 0.45 m; a tight crossing (slack <= 1) needs centimetre alignment the sweep proved only at the centre. Harness now hits intermediate waypoints to 0.2 m (run 34); Engine fix (2026-09-09): `PathResult::arriveRadius` per waypoint from the graph (`NavGraph::arrivalRadiusAt`: 0.15 m on a cell with a tight EXISTING crossing, 0.5 m elsewhere; a missing edge is not tightness), filled by PathService after smoothing; `ScheduledBehavior` advances at the waypoint's own radius and no longer applies the 0.4 m 'close enough' stop to intermediate waypoints. Test `NavGraphMicroTest.ArrivalRadiusIsTightAtTheDoorAndLooseOnOpenGround` (47/47 nav suites). StoryDrivenBehavior now uses the same per-waypoint radii (2026-09-10); PatrolBehavior (its own node rule) and CombatBehavior (1.2 m, open-ground combat movement) keep theirs - noted. Shipped since run 35; StoryDriven in run 46. | FIXED (Scheduled + StoryDriven) |
| G-80 | S1 | Authoring (Ravenmere) | A scene-transition floor trigger must not sit on a natural walking line | The cellar hatch region (-27,8) was on the storeroom line from the street door to Bram at the stair rail: walking to Bram fell into the cellar mid-step (`to_cellar` fired while the harness was still steering to Bram, which it then did inside the cellar - stuck at (6,18.9)). Moved to the storeroom's north-west corner cell (-27,9) against the street wall; Hobb's line updated. Run 34 fired it AGAIN from the moved cell: the harness's `calibrate()` tapped W then D on every precise walk, drifting the player ~0.75 m west per call, from the door line onto the hatch 2.5 m away (the AABB test is exact; the player really was there). Harness calibration is now net-zero and not per call. Engine-side: regions that change scenes want an interaction gate (a hatch object you use), not a floor plate - the trigger vocabulary has only `entity_reached_region`; a 1 m floor plate in a room people mill about in WILL be stepped on. Run 37 (net-zero harness, walk trace): `to_cellar` fired while the harness read the player oscillating at (-24.4..-24.7, 8.3..8.7) - 1.3 m EAST of the region (x -27..-26, z 9..10) - so either the trigger saw a different entity/position than `/api/state` reports, or the region test is not what it looks like. `TriggerSystem` now logs the entity and position that satisfied a region. Run 38: the involuntary firing did NOT recur (Bram reached, all three legs arrived); the deliberate hatch walk fired at (-26.46, 17.33, 9.36) - inside the region, log correct. The runs 36/37 firings stay unexplained (intermittent; the log line is armed for the next occurrence). | FIXED (authoring + harness); engine gap OPEN; intermittent firing WATCH |
| G-81 | S0 | Runtime (EngineRuntime + character) | A stalled frame must never be replayed as seconds of motion | On town re-entry the frame stalled ~7 s (NavGraph build behind the loading screen) with the harness's walk key still injected; `EngineRuntime::beginFrame` passed the RAW wall-clock delta (the editor clamps to 0.25 s), the character integrated one 7 s axis step whose end point lay beyond every wall in free air, landed at (41,17,92) in unstreamed terrain and fell for ever (y = -1.98 million, `GroundLost` at 14:57:50). Red test `CharacterDoorFunnelTest.ALongFrameNeverTunnelsThroughAWall` (z=28.5 through a wall at 20). Fixes: frame delta clamped to 0.25 s in the runtime; the kinematic resolve substeps any dt > 1/30 s so one step is always shorter than the body; injected input is released when a transition begins (`SceneCallbacks::onTransitionBegin`). Test green (66/66 in the character + scene suites, goldens unchanged). L4 run 34: the town re-entry after the cellar landed the player at (-6.5, 20.8) on the street, it walked into the tavern and paid Hobb (`hobb_paid`, 1006 s) - no fall, no teleport. | FIXED |
| G-82 | S3 | Runtime (SceneManager) | Re-entry restores the CAMERA, not the player, and its log line is garbled | `Restored re-entry position ({:.1f}, ...) for scene '-23.0683'` - the logger has no `{:.1f}`; and `reentryStates_` stores the camera position at unload, while the player respawns at the scene's `player.position` (spawn), so returning from the cellar puts the player back at the town spawn, 30 m from the hatch. Log fixed; the player-position half is a design gap (transition targets / return points). | OPEN (log fixed) |
| G-83 | S2 | Harness + combat balance | The probe must stop on the game-over screen; the farm wolves can kill a level-1 party | Run 38: the wolves fight ran 27 rounds (the alpha wolf at 3 hp survived; the player burned `cure_wounds_self` until `no slots`), the player DIED at 16:58:42 (`Player died - game over`, screen `game_over`, health 0) and the harness kept 'walking' a dead character for 400 s (`steer_stuck` at (22.0,12.7), every trace point identical). Runs 36/37 won the same fight in 10 rounds - dice variance decides a level-1 party vs 4 wolves. Harness: fight() now aborts on `alive:false` / screen `game_over` and records `game_over`. Run 39: died AGAIN to the wolves (16 rounds, health 0, `game_over`), and the harness's in-loop death check did not fire because the fight loop exits on 'combat over' before it - the check now also runs after the loop. Two deaths in three runs at the same encounter: the wolves (alpha + 3, 140 hp) are over budget for a level-1 party without a controllable companion. Balance is the designer's call (encounter budget; G-01 char creation / G-04 party control related). Run 42: died a THIRD time (25 rounds; the harness aborted correctly with `game_over` this time). 3 deaths in 5 runs at one encounter = the encounter is over budget, not bad luck. Run 45: a FOURTH death (22 rounds). 4 deaths in 7 runs at the wolves. BUDGET (SRD encounter building): three `wolf` stat blocks (CR 1/4, 50 XP each, pack tactics) = 150 XP x2 for a group of three = 300 adjusted XP against a single level-1 character whose 'deadly' threshold is 100 XP - three times deadly when the companions are not in the fight (the harness's recruit steps hit `unknown action: party`, G-28, so whether Wren/Bram join is unobservable). Designer's options: two wolves, or gate the alpha behind Wren's recruitment, or make the party observable and fix G-28 first. | HARNESS FIXED; balance OPEN (4 deaths / 7 runs; 3x deadly by SRD budget) |
| G-84 | S1 | Runtime (scene load) | A world scene whose database yields 0 chunks must not spawn the player into the void | Run 40 (09:45): re-entering the town logged `Found 0 chunks in database` -> `No chunks found` -> `Building physics ... for all 0 chunks` -> `[ERROR] No terrain occupancy grids registered ... characters will fall through all terrain` - and then spawned the player anyway, who fell to y = -91,000 while the harness steered a falling body (the DB was empty because a concurrent regen had deleted and was rebuilding it - an ops error - but the runtime's response to an empty world must be a refusal/fallback with a visible error, not a void). This is exactly layer B's job (`WorldHealth` self-check, increment 5): zero terrain under the spawn = fail loudly. | OPEN |
| G-85 | S3 | Structure gen (locations) | A building's location marker must sit in front of its street door | Regen #14 gate log: house_14 (hall_house @40,4, 16x8, cross-passage doors at x=44 on both long walls) has its location at (40,17) - the footprint's WEST corner column, 5 cells out on the street - not 2 cells out from the door at (44,11). The doorsteps unit and the walkability gate both keyed off the marker; the gate now finds the door along the marker's wall itself (the opening whose wall and inside cells seat the agent, nearest the marker). The doorsteps unit still trusts the marker (a stoop could land on the wrong column); the forge's location placement for this typology needs the same look. | GATE MITIGATED; forge OPEN |
| G-86 | S2 | NPC placement (legacy NavGrid) | The nearWall relocation must not lift an NPC onto whatever is topmost | Run 43 WorldHealth: `UNREACHABLE npc 'Gerrit' at (39.5, 22, 16.5)` - the shipped game logged `Relocated NPC 'Gerrit' from nearWall (40,16) to (39.5,22,16.5)`: the relocation reads the legacy 2.5D NavGrid's topmost-voxel surface (G-60) and put Gerrit 5 m up on a canopy/roof over the street, where no walker can reach him. First defect found by layer B, not by a playthrough. Fix (2026-09-10 11:50): in micro mode `NPCManager::buildNavGrid` relocates on the NavGraph - an NPC whose own cell has a walker surface within a cube of his feet stays put; otherwise the nearest column (spiral <= 3 cells) with such a surface takes him at ITS feet level; the legacy nearWall/topmost path is kept for cube mode only. Test `NavGraphMicroTest.AnNpcBesideATallTrunkStaysOnTheGround` (green on the fix; the RED is the live run-43 log `Relocated NPC 'Gerrit' ... to (39.5,22,16.5)`, not a unit run against the old code). L4 run 44 (11:25): `WorldHealth: reachable 14/14, terrain under spawn yes, graph ok` on the town's first load - Gerrit reachable, no canopy relocation. | FIXED |
| G-87 | S1 | Scenes (engine) | Scene transitions must be VISIBLE and deliberate, not invisible floor plates | User direction 2026-09-10 ('area transition sites should be graphical or visual'). Every transition region was an invisible AABB; the only way to find the cellar was Hobb's sentence and the only way to leave town was to walk off the map edge. Engine: `TransitionMarkers` plans a prop per transition region (trapdoor ON a hatch, ladder ON a stair well, waystone at the VERGE of a road exit; authored `marker`, `marker_offset`, `none`), placed by the loader on every load; `when.interact: true` fires on the interact key inside the region (E in the shipped game). New templates `architecture/trapdoor`, `architecture/waystone` (catalog regenerated). L4 run 47 (13:44): standing on the hatch region no longer transitions; the E press there does (`hatch_interact: scene cellar`). The props were NOT placed in run 47 - the shipped scaffold lacked a PlacedObjectManager (G-65 root cause, fixed in the scaffold); Run 48 (14:05) with the scaffold fix: `Transition marker 'trapdoor' placed for trigger 'to_cellar'`, `'waystone' placed for 'to_farm'`, `2 placed, 0 failed` on the town load. VISUAL L4 (run 48, `docs/evidence/ravenmere/inc7_waystone_east_exit_run48.png`): the waystone stands 1.7 m tall on the grass verge at the east road's end, cap band visible, seated on the ground - photographed at night, so it reads as a dark silhouette; the trapdoor needs an editor shot with a controlled camera (the in-run framed shot from inside the tavern caught the player's back in an unlit room). Trapdoor VISUAL (editor, noon + a temporary point light, `inc7_trapdoor_storeroom_v1_pointlight_editor.png`): seated flush on the storeroom's sub-cube floor (micro seating works), ring and hinge straps visible - but the plate was cut from `Wood`, the same oak planks as the floor, and all but vanished; re-authored in lapped `WoodPlanks` with a raised iron rim: v2 (`inc7_trapdoor_storeroom_v2_pointlight_editor.png`) reads as a hatch at a glance - pale planks, dark rim, ring, straps, flush with the floor beside the storeroom press. Without the extra light the storeroom is black at noon (G-89). | FIXED (interact gate, placement, both props visual L4) |
| G-88 | S2 | Scenes / world extent | A scene's world must extend past its exits; the road must not lead into a black void | Noon editor shot at the east exit (`inc7_waystone_east_exit_noon_editor.png`): the waystone stands at x=63, the last generated column of the town world, and everything beyond it is unrendered black - the road to Hollin Farm ends at a wall of nothing. The transition region sits at the generated edge because the town scene's world range is the settlement footprint plus margin. Fix (2026-09-10, two halves): (1) WorldHealth now samples the ground 12 cubes past every region trigger in all four directions and reports `VOID beyond exit 'to_farm' within 12 m to +x` (`exits_facing_void` in the report, a selfcheck defect record) - the void is detected on every load; (2) the Ravenmere regen's town world grows one chunk past every exit (x -96..95, z -64..95). Test `WorldHealthTest.AnExitAtTheWorldsEdgeIsReportedAsFacingAVoid`. The generator-side rule (a scene's world extent derived from its exits) is still owed to the world model. | BUILT. Regen #16 (2026-09-10 13:42, world block chunks x −3..2 / z −2..2): noon shots from the exit (`inc7b_east_exit_terrain_regen16.png`, `inc7b_east_exit_from_road_regen16.png`) show grass and trees for 30+ m past the waystone; the flat test world's own edge (x=96) is still visible on the horizon — the generator-side world-extent rule (world must reach past every exit by construction) stays open. Shipped `exits_facing_void` = run 49 (pending). |
| G-89 | S2 | Lighting / interiors | Generated interiors are unlit at noon | Every interior camera shot (the storeroom for the trapdoor, in the shipped run and in the editor at 12:00) is pitch black: no sunlight reaches the rooms and the structure generator places no interior light sources in the tavern's service rooms. A player cannot see the trapdoor - or anything - indoors without a torch. The town registers 81 emissive voxel lights (taproom-style fixtures), so lighting exists as a system; the generator simply places none in service rooms, and the ambient term (max 2.0) never reaches a skylight-occluded interior. Fix (2026-09-10): `recipeFor` appends a `wall_lantern` (canon: sconce mount 1.52-1.83 m) to any room recipe with no lighting-pass piece, so every room is lit by construction; recipes that light themselves keep their own fixtures. Test `LightingPass.EveryRoomRecipeCarriesALight` (RED on service/kitchen/bedchamber/hall/salesroom/forge/bakehouse/shambles/default); BuildingHarness furnish-plan goldens re-pinned for the two-room corpus entries. L4 pending: regen #16 + an editor noon shot of the storeroom with no extra light. | BUILT, ROOT CAUSE FOUND on regen #16. The recipe rule alone was not it: the regen log shows `furniture did NOT fit: wall_lantern` in NINE rooms (bakehouse, taproom ×2, chamber ×2, chamber_1, chamber_2, storeroom, backroom — auditor-recounted; an earlier draft said twelve) — the sconce competed with the chest/barrel for a FLOOR cell and lost, so the room's only light was dropped. Engine fix: wall-mounted pieces (`mountFor == Wall`) reserve no floor cell and hang above any occupant whose measured height stays under the 14-micro mount (`occupiedTop`; unknown height = tall); `FurniturePlacerTest.WallLanternHangsAboveLowFurnitureButNotOverTallFurniture` (mutation-checked: rule off → FAIL). Measured on regen #16 in the storeroom that DID get its sconce: mean viewport luminance 30.0 with the sconce vs 22.0 with its two emitters disabled (`inc7b_storeroom_selflit_regen16.png` vs `…_lantern_off_control_regen16.png`, same camera) — lit, dim; the wall beside the sconce reads 61.4. L4 regen #17/#20: `wall_lantern did NOT fit` 12 → 0, `place_lights` dark rooms 4 → 0, 31 sconces in the DB (was 26); storeroom luminance 40.9 with its sconce vs 21.7 with the sconce's two emitters disabled (`inc7b_storeroom_hatch_regen20.png` vs `inc7b_storeroom_sconce_off_control_regen20.png`). FIXED. |
| G-90 | S1 | Generator / designer anchors | The furnisher buried the cellar trapdoor under a barrel | Regen #16 placed-object list: `barrel_6` bbox x −28..−27 z 9 covers the `to_cellar` trigger cell (−27,9); the trapdoor prop was placed at the same cell (`inc7b_storeroom_selflit_regen16.png` shows the barrel where the hatch is). The settlement generator never hears about the game definition's trigger regions, so a designer anchor is just another wall cell to it. | ENGINE: `KeepClearBox` (`core/KeepClear.h`) — the editor turns `TriggerSystem::regionTriggers()` into keep-clear boxes for both `build_settlement` and `build_structure` deps; `FurniturePlacer::keepClearRects` reserves the covered cells on the storey the box meets (like stair wells); `FurniturePlacerTest.KeepClearBoxReservesItsCellsOnItsOwnStoreyOnly`. The loader now also REPORTS any placed object covering a marker cell (`markerObstructions`). L4 regen #17/#20: the hatch cell is covered by nothing but the trapdoor itself (barrels moved to the east wall); `inc7b_storeroom_hatch_close_regen20.png` shows the plate, rim and ring flush on the floor. FIXED. |
| G-91 | S2 | Loader / idempotence | Transition markers were placed again on every scene load | Regen #16 placed-object list: `trapdoor_1` + `trapdoor_2` on one cell, `waystone_2` (y 18) seated ON `waystone_1` (y 17) — the regen loads the town twice (before and after the build) and a shipped game re-places on every load of a DB that already holds the props. | ENGINE: `markerAlreadyPlaced` — a same-template object within one cube of the planned column is kept (its voxels verified via `occupiedMicro`; a record built over with no voxels left is dropped and re-placed); log line now reads `N placed, K already present, F failed`. `TransitionMarkersTest.AMarkerAlreadyInTheWorldIsRecognisedAndNotPlacedTwice`. Found an ODR crash on the way (a second `Phyxel::Core::PlacedBox` with another layout → 0xc0000005 in a pure function; renamed `MarkerSiteBox`). L4 regen #20 + editor reload: `Transition markers: 0 placed, 2 already present` with `trapdoor_2 intact check: 121/121` and `waystone_1 … 145/145`; DB holds exactly one of each. FIXED. |
| G-92 | S1 | Persistence / placed objects | A placed object's micro pose was never saved | Found chasing G-91 on regen #17: the loader's "is the marker still there" check never ran because `PlacedObject::toJson` dropped `microAnchor`/`placedAtMicro` — every fixture loaded from a world DB reads as cube-placed. Consequences beyond markers: removing a loaded fixture falls back to clearing whole CUBES over its bbox (the wall behind a stool goes with it — the exact defect the micro-erase path was written to end, but only for objects placed in the same session). | ENGINE: `placed_at_micro` + `micro_anchor` persisted and read back (legacy records load as cube-placed); `PlacedObjectMetadataTest.MicroPoseRoundTripsThroughJson`. The marker intact check now verifies the record's OWN cells at its pose — material must match the template's AND the cell must be exposed (`ObjectTemplateManager::countTemplateMicroPresent`): a hatch that survived INSIDE the floor slab (every cell occupied, none visible) is replaced, seated on the real surface. L4 regen #20: post-build reload logs `trapdoor_1 intact check: 0/121 exposed … replacing it` (the pre-build hatch had survived INSIDE the slab), re-seats it at micro y 156 (on the slab), keeps the waystone (`145/145`); the saved records carry `placed_at_micro`/`micro_anchor`. FIXED. |
| G-93 | S1 | Loader / world generation | An inline `world` block regenerates over a scene DB that already holds the chunks | G-48 again, by a new road: regen #18's post-build reload ran while the regen's temporary inline world block was still in `game.json`; the scene loader logged `World: generated 30 chunks (Flat, seed=7)` OVER the built town and the following save wrote the flat world back (town.db blobs 7.7 MB → 3.8 MB; the hatch re-seated on bare ground at micro y 153). Only one editor path (`Application.cpp` project open) set `skipTerrainGeneration`; scene transitions and the shipped scaffold never did. | ENGINE: `GameDefinitionLoader` refuses to generate when ANY target chunk already exists in the world DB (`WorldStorage::chunkExists`) unless `world.regenerate: true` — WARN `inline world block is IGNORED (the DB is the source of truth...)`, `result.worldGenerationRefused`. Regen script reordered (reload after the block is removed). L4 regen #19/#20: with the block still present the loader logs `World: 30 of 30 chunks are already saved … IGNORED` and does not regenerate (the #19 damage came from G-94, not from generation). Run 49 (shipped) then showed the guard's other half: the refused path returned WITHOUT loading the region, and nothing else loads a fixed-region scene's chunks — the second farm visit had 2 of 9 chunks in memory (the two a marker check touched), the NavGraph covered XZ [0..31]×[0..63], `2 exit(s) facing a void`, and the player wedged at (8.4, 22.4) on the way to Oswin; the barrow was never reached. Fix: the refused path loads every saved chunk of the range (`World: loaded N saved chunks of the region from the DB`); `GameDefinitionTest.InlineWorldBlockLoadsSavedChunksInsteadOfRegenerating` (headless ChunkManager + temp DB: generate, edit, save; reload → refused, both chunks in memory, the edit intact). Run 50: the region loaded (`loaded 9 saved chunks`, WorldHealth 9/9) but see G-95. Run 51 (2026-09-11, shipped): second farm visit → Oswin arrived, barrow entered, full playthrough. FIXED. |
| G-94 | S1 | Engine / chunk lifecycle | `createChunk` shadowed a SAVED chunk with an empty one, and the save wrote it back | Regen #19: the G-93 guard held (WARN fired, no regeneration), but the post-build reload's transition-marker pass ran BEFORE the town's chunks had loaded from the DB. The intact check read air (`0/121 exposed`), the stale-record erase reached `getOrCreateChunk`, which created an EMPTY chunk over the saved tavern chunk, and the following save persisted it: chunk (−1,0,0) 1,877,455 → 775 bytes, chunk (1,0,0) 1,016,833 → 135 bytes. Every get-or-create caller (5 in `ObjectTemplateManager`, the loader, `VoxelManipulationSystem`, `ensureChunkAt`) shares this hazard on any DB-backed scene. | ENGINE: `ChunkManager::createChunk` now checks `WorldStorage::chunkExists` first and LOADS the saved chunk instead (WARN `LOADED it instead of creating an empty one`; refuses outright if the load fails) — `ChunkStreamingManagerTest.CreateChunkLoadsASavedChunkInsteadOfShadowingIt` (mutation-checked). The loader also calls `ensureChunkAt` for a marker's column before reading/writing it, so the intact check and seat scan see the real world. L4 regen #20: three `createChunk(…): chunk is saved in the world DB - LOADED it instead` lines at the post-build reload, markers verified against the built world, tavern chunk 1,878,181 bytes after the save (1,877,455 on #17 + the hatch), waystone chunk unchanged. FIXED — `CreateChunkLoadsASavedChunkInsteadOfShadowingIt` green, mutation (guard off) red, 102 green restored. |
| G-95 | S1 | Engine / chunk lifecycle | Chunks loaded from the DB by the new load paths had no faces and no collision | Editor, 2026-09-10 17:25: the second farm load went through the G-93 refused path (`loaded 9 saved chunks`) and every farm NPC fell to y ≈ −19,600 (Dunstan −19,788; API `/api/npcs`); the viewport was black at the spot. `ChunkStreamingManager::loadChunk` defers faces and collision to a bulk pass (`buildAllChunkPhysics` + `rebuildAllChunkFaces`) that only the no-world-block scene path runs — the generation path finalizes each chunk itself and the two new load paths (G-93 refused path, G-94 `createChunk` load) did neither. Why the shipped run-50 player still stood on the second farm is NOT established (the shell may run its own physics pass for the spawn chunk); the editor reproduction is the evidence, and the wedge on the way to Oswin is still open. | ENGINE: both load paths now finish a loaded chunk exactly as generation does (`rebuildFaces` + `updateVulkanBuffer` + `forcePhysicsRebuild`; `createChunk` does it only when the streaming manager is in bulk mode). `GameDefinitionTest.InlineWorldBlockLoadsSavedChunksInsteadOfRegenerating` now also asserts faces > 0 and `getOccupancyGrid().isCubeFilled` for the saved cube (mutation: finalize loop skipped → red). Fix folded into ONE finalizer, `ChunkManager::finalizeLoadedChunk` (the streaming stream-in lambda now calls it too): physics-world registration + `createChunkPhysicsBody`, `forcePhysicsRebuild` for the chunk's own grid, voxel maps, and a synchronous mesh on the load paths. L4 editor 2026-09-11 09:42: after the refused-path farm load all five NPCs read y 17.0 after 10 s (were −910 / −19,600). Run 51: complete playthrough, second farm visit clean. FIXED. |
| G-96 | S2 | Character / doorways | Intermittent wedge at the tavern's taproom→kitchen door | Run 50 only: on the way from Bram to the cellar hatch the player pinned at (−22.0, 1.6) — the east end of the taproom/kitchen partition, where the doorway is (`inc7c` shot: `town_wedge_from_taproom`) — pressing S+D for 11 minutes of re-plans; runs 49 and 51 walked the same route from the same DB without incident. Not reproduced; a wandering NPC in the 0.78 m doorway is the leading suspect (Hobb idles 2 m away, Wynn wanders), but nothing in the run-50 engine log names it. | OPEN. Needs: NPC positions logged when the harness reports a wedge (harness), and the doorway-yield rule for NPCs (engine: an NPC must not stand in a doorway when a character approaches — plan increment 2/3 territory). |
| G-97 | S3 | Diagnostics | `/api/walk/probe` reports `fits_at_start:false` everywhere, at any feet height | Editor 2026-09-11: probes at (7.9, y, 22.2) on open flat farm ground for y = 16, 16.5, 17 and at (−22, y, 1) in the tavern for y = 17, 17.5, 18 all return `fits_at_start:false, reached:0`, every first step `fits:false` — the tool cannot be used to answer "is this cell walkable" from the API, which is exactly what G-96 needs. | OPEN. Check what the endpoint expects for `y` (feet cube vs. micro vs. float) and add a unit test that probes a known-open flat cell through the same code path. |
| G-98 | S1 | Engine / chunk physics | Every character in a revisited generated scene stands one cube INSIDE the floor | Manual test 2026-09-11 (barrow, combat): player and monsters clipped into the StoneTiles floor. Measured: the editor's barrow has everyone at y 17.0 on the 17.0 floor with foot boxes at 17.07 (`get_bone_positions`), and headless tests put humanoid/skeleton soles within a microcube of the floor — so neither the controller nor the rig offset is wrong. A second shipped instance (`--test 8104`) read every barrow character at **y 16.0**: one cube below the floor, standing on the terrain under the tiles. Chain: the barrow is a REVISITED scene, so its chunks come from `barrow.db` through `ChunkManager::loadChunk` → `Chunk::initializeForLoading`, which turns physics BULK mode on; the new shared finalizer never turned it off; the definition's `fill … replace:true` floor then ran on bulk-mode chunks, where a voxel REMOVE reaches collision but an ADD is skipped (`ChunkVoxelManager`: `m_removeCollision` unconditional, `m_addCollision` gated) — a collision hole the size of the floor. Reproduced red headlessly with the exact path (`CharacterStandingTest.ReplaceFillOnADbLoadedChunkIsSolidGround`, and the bulk-mode variant). The real loader on the real barrow definition is clean on a FIRST visit (`TheShippedBarrowFloorIsSolidThroughTheRealLoader`) — which is why the harness runs never saw it. | ENGINE: `finalizeLoadedChunk` takes the chunk out of bulk mode after registering its grid (one line). Also added: `DrawnFeetMeetTheFloorForEveryShippedRig` (humanoid/skeleton pass; wolf_meshy sole 0.11 below — a separate one-microcube rig-offset defect, G-99). L4 = shipped instance barrow y 17.0 + feet shots; the user's running game needs a restart to pick it up. |
| G-99 | S3 | Character rigs | `wolf_meshy` sole sits one microcube below its feet position | `DrawnFeetMeetTheFloorForEveryShippedRig`: lowest drawn voxel box − floor = −0.112 for wolf_meshy (humanoid, skeleton_warrior, forge_rodent within tolerance). Non-imported rigs ground on the lowest BONE, not the lowest voxel. | ENGINE: every rig with voxel boxes now grounds on its lowest box, cancelling the draw's 0.05 lift (the imported-rig rule made universal). Measured with the renderer's exact origin (lift included): before, the humanoid's sole floated 12 cm above the floor and the wolf's sat 6 cm inside it; after, all four shipped rigs are within one microcube. Goldens re-recorded: only the controller-height scalar moved (humanoid 0.935 → 0.950, halfling 0.535 → 0.700, ogre 1.351 → 1.381 half-heights — the capsule now spans the rig's voxel boxes, not its bones; 3,190 pose lines per file unchanged). Also made bulk-mode collision deferral symmetric (a remove during bulk is deferred like an add). FIXED. |
| G-100 | S2 | HUD / layout | HUD text overflows its panels and panels overlap | Manual test 2026-09-11 (`manual_test_1.png`): the Objectives text runs past its fixed 340×220 panel, the Initiative list loses its last rows, the spell buttons sit on the Initiative panel, the round banner sits on a nameplate. Cause: the HUD is authored as fixed rectangles (`resources/ui/default_hud.json`), `UIPanel` has no notion of content height, and nothing checks the result. | ENGINE: a layout pass — `UIWidget::measureHeight` (labels wrap and count lines, repeaters sum rows, panels stack children), `UIPanel::applyAutoSize` (JSON `autoSize:true`; `maxSize:[w,h]` caps the height and turns the panel scrollable) run before every anchor resolution — plus `ui/UILayoutLint.h`: every child a fixed panel would cut off and every pair of visible panels that overlap, exposed as `UISystem::lintLayout` / `POST /api/rpg/ui_lint` and written to `playtest/defects.jsonl` by the shipped shell whenever the visible screen set changes. HUD authored: objectives (cap 360 → scrolls), initiative (cap 420), action, banner and turn panels auto-size. Tests `UILayoutTest.*` (3: wrapped line count; grow + cap → scroll; the lint sees the old cut-off and the overlap, and not after auto-size). L4 probe exe 2026-09-11: the lint first reported the four defects the user saw (health over hotbar 36×60, turn line over the action panel 440×34, hotbar rows clipped 4 px, spell bar on the initiative list) plus my own regression when I moved the action panel (health/action 340×60) — authoring took `ui_lint` to 0 in town and 0 in the barrow fight; fullscreen overlays are excluded from overlap checks and unnamed panels report by screen name. FIXED. Open follow-up: the main-menu `menu:main` panel is 120 px too short for its buttons (game.json authoring; logged by the shell lint). |
| G-101 | S2 | Combat / controls | No visible way to select a target or attack | Manual test 2026-09-11: "I should be able to click on characters to select them for attacks, I have no idea how to actually do an attack." The mechanics existed (left click on a foe = attack, on the ground = move, hover lights the nameplate) but nothing on screen said so and nothing marked the target in the world. | SHIPPED SHELL + ENGINE: (1) the pointer says what a click does — `WindowManager::setCursorShape` (crosshair over an attackable foe, hand over walkable ground on your turn, arrow otherwise); (2) a ring at the feet of the hovered/selected foe (red hostile / green ally) and a softer one under the acting combatant, drawn through the debug-line pass with the debug ray path forced off; (3) a hint line in the action panel (`combat.hintText`: "Your turn: click an enemy to attack, the ground to move" / "Click to attack Skeleton" / "Cast X: click a target"); (4) an **Attack** button that swings at the selected or hovered foe through the same `requestAttack` path a click takes. BUILT; L4 = probe exe: `player_pick` on a skeleton → ring + hint, `ui_click` on Attack → attack in the combat log, lint 0. Cursor shape cannot be captured in a screenshot; verified by code path only. |
| G-102 | S2 | Combat / turn controller | Attacking a foe out of reach does nothing | Probe L4 2026-09-11 12:44: `player_pick` on the Cultist 12 u away resolved "attack", the Attack button logged `refused`, no swing, no movement, the turn budget untouched — the player has to guess that they must first click the ground within 5 ft of the foe. BG3 walks you into reach and swings. | ENGINE: `PlayerTurnController::requestAttack` on an out-of-reach foe now requests a move to a point 0.75 × reach short of the foe when the movement budget covers it and swings on arrival (`m_approachTargetId`, resolved in `tick`); refuses only when the walk exceeds the movement left. `PlayerTurnControllerTest.AttackOutOfReachApproachesThenSwings` (5 u foe: approach → arrive → swing → damage → movement spent; the old `AttackRejectedOutOfReach` keeps its teeth at 20 u > 30 ft). Hint line shows "Closing in on X...". L4 probe exe 2026-09-11 12:52: `player_pick` on Skeleton2 at 3.1 u (reach 1.5 u) → the player walked 1.78 u and the swing landed (13 → 6 HP), the action spent, movement 9.1 → 7.6; the hint read "Closing in on Skeleton2..." during the walk. FIXED. |
| G-103 | S2 | Engine / render | Shipped games have no world-space line pass | Probe L4 2026-09-11 13:0x: the target rings queued by the shell never appeared. Three gates in a row: `RaycastVisualizer::m_enabled` (its own flag, off by default), `RenderCoordinator` cleared transient lines BEFORE upload (so anything queued during the game update was wiped) and never called `updateBuffers` itself (only the editor did), and the shipped shell passed `nullptr` for the visualizer — only the editor ever constructed one. | ENGINE + SHELL: the render coordinator uploads debug lines before drawing them and clears them after; the shell constructs and initializes a `RaycastVisualizer` and hands it to the coordinator; the ring code enables the visualizer and forces the debug ray path off. A fourth gate found on the way: the coordinator's F5 flag `raycastVisualizationEnabled` also HIDES the player (segment-box debugging) — my first ring build set it from gameplay and the player vanished from the combat frames; lines now draw whenever the visualizer has any (`hasLines`), and gameplay never touches the F5 flag. L4: player visible again; the ring itself is still missing → G-105. |
| G-104 | S2 | Items / inventory model | Minecraft item model: every item stacks to 64, coins are a hotbar item | Manual test 2026-09-11: a white block "30" in the hotbar (30 `gold_crown` items from the relic trigger), `Inventory::addItem` hard-coded `64`, `ItemDefinition` defaulted `stackable=true, maxStack=64`; four of Ravenmere's five loot ids are not even in `items.json`. | ENGINE: stacking is per-item DATA — unauthored items are one per slot, consumables default to 20 (SRD: a quiver holds 20 arrows), authored `maxStack` wins; `Inventory::stackSizeFor` reads the registry (unknown ids = 1). `PlayerProfile.currencyData` persists a `Currency`. SHELL: `give_item` of gold/gp/gold_crown/coins credits the wallet (`Currency::addCoins`) and logs "Gold received: N gp"; the HUD shows `player.goldText` under the health bar. Tests `InventoryTest.StackSizeComesFromTheItemDefinitionNotSixtyFour`, `ItemDefinitionTest.UnauthoredItemsDoNotStackAndConsumablesStackToAQuiver`. BUILT; L4 = relic trigger → "30 gp" on the HUD, no coin block in the bar. Still open in this increment: the bar itself is still the material hotbar (BG3 action bar next). |
| G-105 | S3 | Engine / render | Target rings still do not draw in the shipped build | After G-103's four gates (visualizer constructed in the shell, its own `setEnabled`, upload-before-draw with clear-after, stale vertex count reset) and decoupling from the F5 flag, a straight-down probe shot over a selected skeleton (13:34) shows the nameplate bracket and readout but no ring, and no acting ring under the player. Remaining suspects: the debug-line pipeline's view/projection descriptor in the shipped path, or the pass order (lines drawn before the scene is composited). | FIXED (2026-09-16, engine + shell): the ring is no longer a debug line — it is drawn THROUGH THE UI as 36 dots projected from a 0.55 m circle at the feet (`UISystem::addWorldMarker`, window px → logical), rotating and pulsing; gold on the selected/hovered foe, blue under whoever is acting. The first build queued the markers but drew them only on the no-HUD early-return path (never in combat) — moved into the main render pass. The yellow corner bracket around the plate column was retired at the same time (the user: "a more obvious visual marker, something like a bright animated circle on the ground"). Probe L4 run 3 (`F5_ring_selected`): gold dotted ring under the selected skeleton, blue ring under the player, no bracket. |
| G-106 | S2 | HUD / action bar | The "action bar" was a Minecraft material hotbar with nothing in it | Manual test 2026-09-11: "what the fuck is the action bar for if we don't put things in it". The bottom bar was the editor's creative material hotbar (nine slots, 64-stacks); the things a player can DO lived in three places (a code-built spell bar, Attack/End Turn buttons in a side panel, click semantics nobody could see). | ENGINE + SHELL: a data-driven BG3-style action bar — `HudDataContext::setAction` + JSON `actionBind` let a repeater of buttons take label/enabled/armed state from rows and route clicks to one handler with the row's record; `hud_action_bar` (bottom centre, in combat) lists Attack, every spell (armed = ember, depleted = disabled, slots remaining) and End Turn; the material hotbar is gated on `hotbar.creative`, which a shipped RPG never provides; the code-built spell bar is retired. `UIActionBarTest.RepeaterButtonsCarryTheirRowsActions`. The first probe L4 (2026-09-15) found two more defects behind the bar: the buttons had NO labels (repeater item binds are keyed `item.<field>`; the bar bound `label`) and every click on the bar was swallowed — `UIRepeater` had no `handleClick`/`handleHover`, so `UIPanel::handleClick` stopped at the repeater and returned consumed (`UIActionBarTest.ClicksReachTheRepeatersGeneratedButtons`, red before). Labels wider than a slot overlapped their neighbours; buttons now carry `fitText` (grow to text + padding). L4 2026-09-15 14:33 (probe exe 14:29): bar shows `Attack · Sacred Flame · Guiding Bolt (2) · Cure Wounds (2) · End Turn` with every label fitting; a target pick 3.2 u away approached and swung (miss, roll 7 vs AC 13, `[PlayerTurn]` log), which spent the action so the Attack slot went disabled and a click on it was correctly refused; `ui_click` on End Turn reached the shell handler (`Action bar: End Turn`, engine log `rv_actionbar_l4.log`) and the turn passed to the cultist. Screenshots `shot_1789497162416.png` / `shot_1789497233097.png` (probe tree). Still to come in this increment: consumables (potions) as bar rows, the action-economy pips as icons, and a portrait. |
| G-107 | S2 | Triggers / scene exits | A region trigger tests a point, so a body standing on the edge of a one-cube exit region never fires it | Run 52 (2026-09-15, probe build): the hardened harness stopped at (16.67, 45.92) — 8 cm short of the `back_to_farm` region's z=46 edge — and the transition never fired; run 51 had crossed by luck. A player is 0.44 m wide; the point test made every one-cube-deep exit a precision task. | ENGINE: `TriggerSystem::regionContains(region, pos, bodyRadius)` widens the box on x/z by the agent half-width (`kAgentHalfWidthMicro` = 2 micro = 0.22 m); both the entry and the interact paths use it. `TriggerSystemTest.RegionCountsTheBodyNotThePoint` (8 cm outside fires, 40 cm outside does not). Also: the harness now clears the saved `player_state` row before a run — run 52's "gold_crown ×30" inventory was run 51's save, and the shell migrates legacy coin items to the wallet on load. L4 2026-09-15: run 53 (probe build, `rv_run_20260915_143535.log`, `run53_harness.log`) completed every leg including farm3 and ended on the victory screen (1543 s, 4 fights fought, `ui_lint` 0 on both town entries, wallet 30 gp, relic/grimoire single items). Commit 5f4809a3. |
| G-108 | S2 | HUD / dialogue | The dialogue panel cut off its choices and the health panel sat behind it | Found by running `ui_lint` WITH a dialogue open (the harness only linted town/combat entries): `'dlg_choices' is cut off by 'hud_dialogue' (58 px past the bottom)` and `'hud_health' overlaps 'hud_dialogue' by 274x90 px` — the third choice was invisible, the second wrapped off the panel (probe shot 15:32). | ENGINE + AUTHORING: `visibleWhen` accepts a negated key (`!dialogue.active`, fail-closed like the plain form — `UILayoutTest.VisibleWhenAcceptsANegatedKey`, red first); `hud_dialogue` auto-sizes to its content (cap 420 px), `hud_health` hides during a conversation. L4 2026-09-15: lint 0 with the Reeve's three choices all on screen (`DLG_choices` shot, `rv_dialogue_lint_l4.log`). |
| G-109 | S2 | Engine / picking | Screen-to-world unprojection used NDC z=0, which is INFINITY under the engine's infinite reverse-Z projection | `ClickToMove::pickGround`'s unit test returned no hit from a camera looking straight down at a floor: `inverse(VP) * (x, y, 0, 1)` has w = 0 (the far plane is at infinity), so the ray's far point was garbage. `PlayerTurnController::resolvePick` (combat click-on-ground) used the same two depths. | ENGINE: both unproject two FINITE depths (z=1 near, z=0.5) and intersect a ray, not a segment. `ClickToMoveTest.GroundPickHitsTheCubeUnderTheCursorAndStandsOnItsTop`; `PlayerTurnControllerTest.*` still green. Not measured: whether the combat ground-click was visibly broken before (the harness never clicked the ground). |
| G-110 | S3 | Test API | `combat/screen_of` answers `ok:true` with pixels outside the viewport | L4 2026-09-15: Aldric behind the third-person camera projected to (1935, −257) with `ok:true`; a harness that trusts `ok` clicks off-screen. `pointer_click` now refuses off-viewport pixels (`off_viewport`), so the failure is honest, but `screen_of` should say `on_screen:false`. | OPEN (minor). |
| G-111 | S1 | Camera / controls | Third-person exploration controls were an FPS scheme with a grabbed cursor | User, 2026-09-15: "I really don't like the current system in Ravenmere" — the scenes author only `camera.mode: third_person`, so the shell fell back to `fps`: always-on mouse-look, camera-relative WASD, body locked to the view, left-click swings; with the free pointer cursor every mouse move steered the view. Direction: mimic World of Warcraft (independent of the BG3-style combat camera). | ENGINE: `Input::MmoScheme` ("wow") + `Graphics::MmoRig` are the `third_person` defaults (researched WoW defaults: A/D turn, strafe with RMB held, Q/E strafe, both buttons run, NumLock autorun, Numpad-/ walk, left drag orbits, right drag steers, wheel zoom to first person, camera eases behind the body when moving — CVar cameraSmoothStyle 4, boom collides with walls); `GameShell::defaultSchemeForRig` (mode-only game.json → wow), `applyMmoBindings` (Interact E→F, inventory Tab→B), `controls.clickToMove` (off, as in WoW; left click selects, right click interacts). Shell: click-vs-drag on both buttons, cursor hidden only while dragging, tactical camera keys gated to the combat rig, wheel uses the rig's zoom range. `MmoControlsTest.*` (11; RMB-strafe rule mutation-checked). L4 2026-09-15 16:31 (probe build, `rv_wow_controls_l4.log`, shots `WOW_*`): W 6.85 m in 1.5 s; A turned body 137° / camera 132°; Q 2.13 m at 95° off heading; NumLock 3.76 m/s until S; both buttons 4.14 m; left click selected Aldric, right click walked and opened his dialogue; ground clicks refused; F talked; lint 0. Not verified live: wheel zoom + wall collision (no wheel injection; rig math unit-tested). |
| G-112 | S2 | Test API / input | An injected tap shorter than one frame was never seen | `InputManager::processInput` ages injections BEFORE the frame's samplers run; at the town's ~8 FPS (126 ms frames, `/api/debug/engine_timing`) a 0.1 s NumLock tap expired unseen, so autorun never toggled — and every sub-frame harness tap was silently lost. | ENGINE: a fresh injection skips its first aging tick (`MmoControlsTest.AnInjectedTapSurvivesTheFrameItArrivesIn`). Tooling lesson recorded, not an engine gap: `localhost` costs 2.1 s per API call on this Windows box (IPv6 first), `127.0.0.1` 0.06 s — the harness already used the numeric address, my scratch L4 scripts did not. |
| G-113 | S2 | HUD / prompts | The interact prompt hardcoded "[E]" and sat on top of the nameplate | User after the WoW rebinding: "the E key doesn't do interaction ... we need a new key for interact" — F was already bound, but the world prompt said "[E] Interact" (and authored prompt texts carry no key at all), and it was drawn at the same head anchor as the nameplate, so the key was unreadable either way (shot 16:33). | SHELL: the prompt names the BOUND key (`getActionKey("Interact")` → `[F]` under the WoW scheme, `[E]` otherwise; authored texts get the key prefixed) and stacks 44 px above the nameplate's head anchor. L4 on the user's exe 16:56: "[F] Interact" above "Aldric", F opens the dialogue (`shot_1789505878873.png`). Recommendation stands: F (next to WASD, free in WoW's map, the common "Interact With Target" bind); right-click on an NPC talks too. |
| G-114 | S2 | Boot | No splash while the exe boots | Manual review 2026-09-16: double-clicking the exe shows a bare window for the ~60 s the texture atlases build (BC7 encode of two 468/180-layer arrays) before anything draws. Engine: the window exists at +0 s but the first frame needs the atlas. Fix = a pre-atlas splash pass (clear + text/logo from a tiny uncompressed texture) or an atlas cache on disk so the boot is seconds, not a minute (the cache also fixes the wait itself). | PARTIAL (2026-09-16) — the WAIT is fixed, the splash is not. The atlas manager already had a BC7 disk cache, but (a) it was keyed on source-file modification times, which every build resets when it copies resources/ into the exe tree, so the first boot after any build re-encoded ~550 MB, and (b) even on a hit the 14 s PNG decode always ran. Now: a content-keyed hash (names, sizes, head+tail 64 KB of each PNG, materials.json bytes — `AtlasManagerTest.SourceHashIgnoresModificationTimesButNotContent`) and a cache-first build that skips the decode when both maps are cached (pixels decoded lazily for the editor's slot/hot-reload paths). L4 2026-09-16 (probe, `rv_boot_one.log` / `rv_boot_two.log`): window → main menu 40.6 s on the boot that rebuilt the cache (new cache version), **3.9 s** on the next boot (2 cache hits, 0 encodes, 0 decodes). Still open: a splash for those seconds and for the one-time encode after a texture change. |
| G-115 | S2 | Scene transitions | No loading screen after New Game | game.json authors `transitionStyle: loading_screen` on every world scene, but nothing in the engine renders it: `SceneDefinition` parses the style and `SceneManager` never reads it (grep: zero uses), so a transition is a hard cut with a frozen frame while the DB streams. Engine gap, not authoring. | ENGINE (2026-09-16): the loading overlay was set and cleared inside ONE `SceneManager::update` on the frame that froze for the load, so it was never rendered on the New Game path (and the shell refused to show it while the menu scene owned the screen). `SceneManager` now PRESENTS the loading screen on one update and executes the synchronous load on the next, so the host always renders a frame with the overlay; the shell drops the menu screen when the loading screen shows and, after the load, HOLDS the overlay until the dirty-chunk backlog drains (min 0.5 s, max 4 s) so the "black first seconds" (G-49) stay hidden. `SceneIntegrationTest.TheLoadingScreenIsPresentedForAFrameBeforeTheLoadRuns` (red before); the fixture's stale scenes (world blocks with no chunk manager, array-shaped camera) repaired on the way — the suite had been failing on the committed code. L4 2026-09-16 11:20 (probe build, `rv_loading_screen_l4.log`): New Game → `screen: loading` at 1.4 s (`shot_1789572020345.png`: "Loading... Ravenmere"), town loaded in 6050 ms under the overlay, dismissed 750 ms after with 0 dirty chunks; town → farm: loading 3.7 s, dismissed 635 ms after the load. |
| G-116 | S2 | Narrative | No intro | The game opens on the town street with no framing. The data-driven `intro` screen overlay exists (`resources/ui/intro_screen.json`, `ScreenState::Intro`) but a menu-start game skips it (the shell only enters Intro on a direct world boot). Simple case: an authored intro text card with Continue on the New Game transition; the cinematic case reuses `CameraPath` (the menu already orbits with it) over the town with the text. | OPEN |
| G-117 | S1 | Wayfinding | No compass or minimap (G-51 restated) | "Directions to go east mean nothing." User direction 2026-09-08 stands: no quest markers, but a compass and a map. Engine: a compass strip HUD element bound to camera yaw with authored points of interest, then a minimap rendered from the NavGraph/terrain heights. | ENGINE (2026-09-16): a `compass` HUD widget (`UICompass`) — a strip of cardinal letters that slides with the camera heading (0 = north = +z, 90 = east = +x; the engine camera's yaw maps as 90 − yaw) with named points of interest as ticks: every scene EXIT (`TriggerSystem::exitRegions`, labelled with the target scene's name — the road to Hollin Farm shows as "Hollin Farm") and every authored location. No quest markers. Hidden in combat (the banner owns the top centre); `visibleWhen` negation from G-108. `UICompassTest.BearingsMapOntoTheStripAroundTheHeading` (mapping, wrap-around, hidden-behind, JSON build + binding). L4 2026-09-16 (probe, `rv_compass_l4.log`, `CP_*` frames): at spawn the camera heading is 225° and the strip reads SE·S·SW·W·NW centred on SW; after turning east (114°) it reads NE·E·SE·S with the needle just past E and the farm-exit tick beside it; `ui_lint` 0 in town and in combat. The HUD lint now runs two frames after a visibility change (it had reported a compass/combat-banner overlap that never exists — both default visible until bound). |
| G-118 | S2 | Animation | Strafing while running plays the forward run | `AnimatedVoxelCharacter` picks `Run` whenever forward < -0.6, before it looks at strafe, so W+Q slides diagonally in the run pose; only the walk band has `WalkStrafeLeft/Right`. WoW blends a diagonal run. Fix in the FSM: a run-strafe state (or lean the run toward the strafe) when |strafe| > 0.1 at run speed. | ENGINE (2026-09-16): the rig has no diagonal-run clip, so — as WoW does — the DRAWN body is angled toward the movement direction while running and strafing (`AnimatedVoxelCharacter::strafeLeanFor`, up to 45°, eased at 10/s, applied at the model-matrix rotate sites only; the logical facing that drives movement, aiming and root motion is untouched). Pure strafes and walk-strafes keep their own clips. `MmoControlsTest.RunStrafeLeansTheDrawnBodyNotTheLogicalFacing`. L4 2026-09-16 (probe, `/api/state` now reports `player.strafe_lean`): W → 0.0, W+Q → −0.785, W+E → +0.785, Q alone → 0.0. |
| G-119 | S2 | Wayfinding / transitions | The transition to Hollin Farm is not signposted | `to_farm` is a plain `entity_reached_region` box at the east edge (x 58..62) with a waystone prop and no prompt: nothing tells the player the road east leads anywhere. Fix: interact-gated marker with a prompt ("[F] Take the road to Hollin Farm") + a signpost prop at the verge; the loading screen (G-115) then makes the jump legible. | OPEN |
| G-120 | S1 | Combat / targeting | Who you are targeting is not obvious, and the interact prompt persists in combat | The selected foe gets only the nameplate bracket + readout (the in-world ring is G-105, still not drawn), and the "[F] Interact" prompt keeps drawing over the fight. Fix: hide interact prompts while in combat; a visible selection (ring or a distinct plate colour + bracket) on the selected target; hover feedback on the plate. | PARTIAL (2026-09-16): interact prompts are hidden while in combat (shell); the selected target has the bracketed, brighter plate with the readout above it (G-122) and the hover/select cursor. The in-world ring at the feet landed 2026-09-16 (G-105 FIXED) and replaced the bracket. |
| G-121 | S2 | Combat / picking | Mouse-over hit area does not match the character | The pick is a 32 px radius around the projected CHEST point (`PlayerTurnController::resolvePick`), so the hot zone is a circle floating over the torso: a character's legs/head miss and empty screen near the chest hits. Fix: project the controller capsule's screen-space box (feet to head, width from the radius) and test the cursor against it. | ENGINE (2026-09-16): `PlayerTurnController::screenBoxOf` projects the character's capsule (feet to head from `getControllerHalfHeight`, ±0.45 m width, 6 px pad) **plus the nameplate column above the head** (second pass, "mouse over still not always on the character": players hover the plate) and `resolvePick` tests the cursor against that box. `PlayerTurnControllerTest.PickHitsTheWholeCharacterNotAChestCircle` (feet, head, chest and the plate hit; 40 px beside the box does not). L4 2026-09-16: feet/head picks → `attack`, 120 px beside → `move`; a pick 40 px above the head anchor (on the plate) → `attack`. |
| G-122 | S2 | HUD / nameplates | Nameplate, AC readout, and the selection bracket overlap | The plate stacks name above the bar and the targeting subtitle below, and the bracket is drawn around the same anchor, so at close range the three collide. Fix: one plate layout with reserved rows (name / bar / readout) sized from the font, the bracket outside the plate's box, and distance-based culling of the readout. | ENGINE (2026-09-16): the plate is one column above the head anchor — targeting readout / name / bar — and the selection bracket frames the whole column; the readout no longer hangs over the character's face where it collided with the head, the bracket and the interact prompt (the prompt now sits 62 px above the column). Render-only change; L4 frame `CB_selected_plate` (this session). Follow-up G-126: plates are world labels drawn OVER HUD panels (Skeleton2's plate across the Initiative panel). |
| G-123 | S1 | Combat / camera | The camera cannot be turned during combat | Mouse-look is suppressed in combat (the capture fix) and only the arrow/Q-T-R-G keys orbit the tactical rig. With the WoW drag-look, a left/right DRAG should orbit in combat too (drag-gated look is safe: no cursor movement leaks into yaw). Fix: the MMO capture rule allows a button-held drag while not driving; the tactical rig honours yaw/pitch as it does now. | ENGINE (2026-09-16): `GameplayCameraController::setLookWhileNotDriving` — for drag-gated (MMO) schemes a held button captures the mouse even while the character is not driven; the shell sets it to `inCombat && !talking`. `MmoControlsTest.ADragOrbitsWhileNotDrivingOnlyWhenTheHostAllowsIt`. The test API gained `inject_input {mouse_move:[dx,dy]}` (the same handler the OS cursor uses). L4 2026-09-16: in the barrow fight, RMB held + 600 px of injected motion turned the tactical camera yaw −135° → −45°. |
| G-124 | S1 | NPC pathing | NPCs walk into building sides and walls | `PatrolBehavior` still routes on the legacy 2.5D `AStarPathfinder`/`NavGrid` (topmost-voxel floor, cannot see doors or sub-cube walls — the same model G-54/G-60 retired for the PLAYER); the engine log shows `STUCK (replan)` + `A* exhausted` for three NPCs every ~1.5 s. Fix: move Patrol/Scheduled behaviours onto the NavGraph (micro mode) via the PathService, as ScheduledBehavior's mover already is for routines. | ENGINE (2026-09-16): `PatrolBehavior` (patrol / wander / follow) routes on the context's NavGraph — async through the PathService when it runs, sync otherwise — with smoothed waypoints and per-waypoint arrival radii, exactly like ScheduledBehavior; the legacy grid is only the no-graph fallback (its terrain probe and teleport last-resort stay grid-only). Wander targets are drawn until one stands on a walkable graph surface (up to 8 draws), so a point inside a building is no longer a goal to walk into. Red test `PatrolBehaviorTest.RoutesThroughASubcubeDoorOnTheNavGraphNotThroughTheWall` (a sub-cube wall with a 7-micro door: the grid sees a wall, the graph the door; the patrol must cross inside the door cell — red before, green after; 44 behaviour tests green). L4 2026-09-16 11:05 (probe build, `rv_npc_pathing_l4.log`, 90 s idle in the town): `STUCK (replan)` 131–264 → **16**, `A* exhausted` 87–275 → **0**, no `no route`; the four moving wanderers covered 33–98 m each. The 16 residual replans are ONE body at (-14.5, 17.1, 11.3) with a valid 7-node route that never advances → G-125. |
| G-125 | S3 | NPC pathing | One wanderer holds a valid graph route at (-14.5, 17.1, 11.3) and never advances | After G-124, all 16 residual `STUCK (replan)` lines in 90 s come from one position just south-west of the Reeve's spot: the graph returns a 6–7 node route each time, the body does not move. Either the capsule collides with something the micro graph treats as walkable there (a stoop / fence residual at that cell) or the resident stands embedded. Next: `navgraph_path` + `navgrid_cell` dumps at that cell on a live probe, and the WorldHealth embedded-spawn check extended to adopted residents. | OPEN |
| G-126 | S3 | HUD / nameplates | Nameplates draw over HUD panels | Combat frame 2026-09-16: Skeleton2's plate rendered across the Initiative panel's title. World labels and plates are drawn after the panels; they should be occluded by (or culled under) any visible HUD panel rect. | OPEN |
| G-127 | S3 | Rendering / stability | One `VK_ERROR_DEVICE_LOST` in the town (11:58, once) | During the first compass L4 the game loop went silent for ~21 s after the town settled, then every `vkQueueSubmit` failed with VkResult −4 and frames were black. Two 60 s A/B idles (with and without the compass panel) on the same build did not reproduce it, and the Windows system log is not readable without elevation. Treated as a driver timeout/reset of unknown cause; watch for recurrence (the harness now records `VkResult=-4` counts per run). | OPEN (observed twice, not reproduced on demand). Seen in two of three `l4_compass.py` runs (a screenshot ~5 s into the town, mid-load), never in: two 60 s idles with/without the compass, two turn-and-walk runs, or a run that captured 14 screenshots straight through the transition, load and settle hold (`rv_shot_during_load.log`, 0 device-lost lines). The compass code is exonerated by the A/B; the capture-during-load theory did not reproduce either. Next: enable the Vulkan validation layer on a probe build and keep the per-run `VkResult=-4` count in every harness log. |
| G-128 | S2 | Animation | Run-strafe lean pointed AWAY from the movement; strafing used the walk clip at run speed | Manual review 2026-09-16 (second pass): "A and Q are the same animation, D and E the same, and backward to the actual direction." Two defects: (1) G-118's lean used +yaw for strafe +1, but the locomotion integrator moves strafe +1 toward the body's right-hand side, which for a +Z-facing model is −x = a NEGATIVE yaw offset, so the body leaned away from where it slid; (2) the run-speed strafe clips (`left_strafe`/`right_strafe`) were gated on the sprint key, which the WoW scheme never sets, so a run-gait strafe played `*_strafe_walk`. (W+A and W+Q both showing the run clip is inherent: the rig has no diagonal-run clip; with the lean corrected W+Q reads as a diagonal run.) | ENGINE: lean sign flipped (`strafeLeanFor` = −atan2(strafe, −forward); unit test expectations re-pinned); the run strafe clip is chosen by the run gait (|strafe| > 0.6) or sprint. L4 2026-09-16 (probe, `rv_feedback3_l4.log`): W+E → strafe_lean −0.785, W+Q → +0.785 (the body now angles toward the side it moves). |
| G-129 | S1 | Screens / flow | New Game after Game Over did not restart | "I died in a fight, hit New Game, it did not restart the game." Game Over → Main Menu shows the overlay main menu; its New Game only flipped the screen state to Playing — the dead player and the old scene stayed. | SHELL: `newGame()` — end the encounter and any conversation, clear inventory, coins, quest progress (the authored objectives are re-added), and scene re-entry positions, never resume the previous run's save, then transition to the manifest's start scene (the player entity is respawned per scene, at full health). New Game from the overlay menu routes there whenever a world scene is active; at boot it starts as before. Engine: `SceneManager::clearReentryStates`. L4 2026-09-16 (probe, `rv_restart_l4.log`): `entity_damage` 999 → `screen: game_over` → Main Menu → New Game → the title scene loads → its New Game → town, `playing`, player 100/100. Known limit: story-engine variables are not reset by a new game (no reset API) — logged with G-116's content work. |
| G-130 | S1 | Engine / shell | New Game after a death crashed the shipped exe | User's run 2026-09-16 16:27 (exe built 14:26): WER `BEX64`, `c0000005`, exception data 8 (execute of address 0 — a call through a null/dangling function pointer or vtable), "faulting module: unknown, offset 0", right after `control bindings` on the post-New-Game town load. Reproduced 3/3 on the 16:40 probe build (feedback-4 L4 runs 1–2 plus run 1's poll; both died at the same point, no WER row, log truncated — stdout is a fully buffered pipe, so the "last line" is not the last thing the engine did). Hypothesis 1 (InteractionManager's cached NPC pointer dangling across `clearNPCs`; its update only runs while a player exists) was closed by `clearCache()` — and the crash still reproduced with it in, so it was NOT the cause. Then 0/5 on the 17:07 build (runs 3–7, one with the window resized first like the user did): timing/layout dependent, NOT fixed by anything proven. | NOT FIXED, NOW OBSERVABLE: `Core::CrashHandler` (engine; installed by `EngineRuntime::initialize` and the editor) writes `crashes/crash_<stamp>.txt` (exception, faulting module+offset, the faulting thread's stack symbolized from the PDB) + a minidump on any unhandled crash; the generated project links Release with `/Zi /DEBUG` so the PDB ships beside the exe; red-before-green `CrashHandlerTest` (a real SEH access violation → the report names `0xC0000005`, `phyxel_tests.exe+0x…`, `#0 ` frames; dump > 4 KB). The next occurrence yields a stack. `clearCache()` stays (a real dangling-pointer hazard, just not this one). |
| G-131 | S3 | Interaction | "[F] Interact" shows on dead characters | Manual review 2026-09-16: the prompt kept drawing over corpses after a fight. `InteractionManager::update` picked the nearest NPC by distance with no liveness check. | FIXED (engine): dead NPCs (`HealthComponent::isAlive()` false) are skipped when choosing the nearest interactable. Probe L4 run 3: after killing the six antechamber foes the player stood 1.2 m from the skeleton's corpse — no prompt (`F5_dead_foe_no_prompt`); living control = the Aldric prompt shot from run 2. Friendly NPCs are invulnerable (`entity_damage 999` leaves Aldric alive), so the dead case needs a hostile. |
| G-132 | S2 | UI / engine | Window resize does not reposition the HUD; the loading screen is not resized either | Manual review 2026-09-16. `UISystem` rendered its 1280×720 logical canvas at the top-left of whatever the swapchain became; input, world labels and nameplates were in window pixels against logical widgets. | FIXED (engine): `UISystem::setWindowSize` + a `Placement` (uniform scale = min(w/1280, h/720), centred/letterboxed) — every screen, data overlay (loading/pause/…) and HUD panel scales and re-anchors; clicks, scroll and hover are converted window→logical; world labels, nameplates and markers arrive in window px and are converted; the UI renderer gets a viewport sub-rect. The shell calls it when the swapchain size changes. `UIPlacementTest` (letterbox math). Probe L4: `window_resize 1920×1080` (new test route) → HUD scaled ×1.5 and anchored to the new corners, `ui_lint` 0 (`F4_resized_1920`, run 2). |
| G-133 | S2 | UI / RPG | No character screen — the player cannot see their stats | Manual review 2026-09-16 ("a character screen would be nice, so I can see my stats"). | BUILT: `ScreenState::Character` + `GameScreen::toggleCharacter()` (resume/goBack/ESC leave it; `GameScreenTest` ×4 red-before-green), the `ToggleCharacter` action (C — WoW's character pane; rebindable, listed in Settings → Keybindings), `resources/ui/character_screen.json` (name/race/class/level/XP, HP/AC/speed/initiative/proficiency, six abilities with modifiers, saving throws, all 18 skills with `*` for proficiency, Close [key]) resolved from `{{sheet.*}}` tokens by the shell out of the live `CharacterSheet` + `HealthComponent`. Two engine defects found by the first L4 shot and fixed: (1) `GameSettings::loadFromFile` replaced the default bindings with the file's list, so every existing `settings.json` showed the new action "(unbound)" — loading now merges missing default actions (`LoadMergesActionsMissingFromTheFile`, red first); (2) data screens (`loadGameScreenInto`) did not hide the HUD underneath, so the sheet's title sat on the compass — they now hide the other screens like the pause overlay and restore them on unload. Probe L4 run 3: C → `screen=character`, the sheet rendered with the cleric's real numbers (STR 14 +2 … WIS 16 +3, AC 18, saves, skills), C → `playing`. |
| G-134 | S3 | RPG / content | The pre-made cleric has no class proficiencies | The character sheet (G-133) shows every saving throw and skill at the bare ability modifier — WIS save +3, not +5; no `*` anywhere — because the shell builds `playerSheet_` from `progression.abilities` only; class saving-throw/skill proficiencies (cleric: WIS+CHA saves, two of History/Insight/Medicine/Persuasion/Religion) are never applied, so every dialogue skill check and save has been rolled unproficient. Same root as G-01 (no character creation). | OPEN. Apply the class's save proficiencies + an authored `progression.skills` list on load (`ProficiencySystem`), and let character creation choose them. |
| G-135 | S2 | Render / lighting | "Low poly shadows" beside the player: hard 1-m blocks on the ground next to the correct fine tree shadow, not aligned to the sun | User's view: `rv_g135_user_window_capture_2.png`. Root cause (shaders): the ground (`voxel.frag`), grass, foliage, characters and glass multiplied their DIRECT SUN term by the traced sky visibility (`phxSkyGate` = skyVis², five near-vertical rays through the voxel occupancy) as well as by the shadow map. That gate is a leftover from the flood-skylight days, when one number gated both ambient and sun; under a canopy it stamps the crown's vertical footprint onto the ground at leaf-voxel resolution, ignoring the sun direction - a second, contradictory shadow. First attempt (grass filter, PCSS) was WRONG and was reverted: it was verified at a pose that did not contain the defect. | FIXED (shaders, user-confirmed in their own view 2026-09-17: "the low poly shadows are gone"): `phxSunGate` in `lighting.glsl` - direct sun is the shadow map's answer wherever the fragment is inside the map (blending through the 12% border fade); the sky gate stands in only outside shadow range. Applied in voxel/grass/foliage/character/transparent_voxel. The sky trace still shapes ambient (sealed rooms stay dark). Evidence: the user's before capture; my probe frames (`rv_sungate_before_A_player_cam.png` / `rv_sungate_after_A_player_cam.png`) show the fine shadow unchanged but never contained the blocky patch - the confirmation is the user's, not mine. |
| G-136 | S1 | Combat / movement | Movement during combat does not go to the cursor click point | Manual review 2026-09-17. Two engine causes: `PlayerTurnController::resolvePick` intersected the camera ray with a PLANE at the player's feet (a click on a slope or ledge landed elsewhere), and `TurnActor` walked a STRAIGHT LINE to the point with a stall guard (a fence or tree stopped it short). Red tests: `TurnActorTest.MovePathVisitsWaypointsInOrder`, `PlayerTurnControllerTest.MoveFollowsThePathProvidersRoute` / `GroundPickLandsOnTheVoxelSurfaceNotThePlayersPlane` (compile-red, then failing on the cut corner). | FIXED (engine + shell): `TurnActor::requestMovePath` walks waypoints in order and debits the distance travelled (stops mid-path when the budget runs out); `PlayerTurnController::setPathProvider` (the shell answers with `ClickToMove::planPath`, the NPCs' NavGraph route) and `setSolidProvider` (the pick becomes the first solid cube along the ray, standing on its top). `combat/player_pick` now returns the resolved point. Probe L4 at the farm (`rv_round4_combat_move.log`): four ground clicks in the wolf fight ended 0.21 / 0.26 / 0.25 / 0.27 m from the resolved point (arrival radius 0.30) after walking 4.97 / 3.44 / 4.40 / 4.92 m. Wart: a click on a tree crown resolves to the crown's top (the walk goes to the trunk's foot). |
| G-137 | S1 | Combat / encounters | Fights only ever start from authored regions: a fled alpha wolf cannot be fought again, a hostile cannot be attacked or spell-cast out of combat | Manual review 2026-09-17: "the alphawolf ran away ... i can then walk up to the alpha wolf but get nothing, and cannot interact. combat probably needs to be a little more sophisticated, and it should probably be possible to cast spells outside of combat which would then trigger combat". Before (probe `rv_aggro_before.log`): walking to a hostile out of combat started nothing; a left click SELECTED it; the only encounters came from `start_combat` regions. | BUILT (engine + shell): `NPCEntity::setHostile` (+ `"hostile": true` in the loader); `Core::EncounterInitiator` (pure rules, `EncounterInitiatorTest`): the hostiles within 12 u of the engaged one join, a hostile that sees the player within 8 u with a clear voxel eye-line engages on its own, an escapee keeps its distance 30 s then may re-engage on sight (a click on it is always a fight). Hostility = the enemy side of every authored `start_combat` in the game (harvested at load) - NOT "has a combat_ai profile": the first build marked the companions Bram/Oswin/Wren hostile (town: 2 hostiles; now 0). Shell: a left click on a hostile engages (walks up if > 12 u), the action bar shows out of combat (spells only) and an armed spell becomes the OPENING CAST on the player's first turn, `start_combat` regions never restart a running fight. Probe L4 (`rv_click_engage_click1.log`, `rv_aggro_after2.log`): arm Sacred Flame out of combat -> click the skeleton 12 m away -> `walking up to engage` -> `npc_Skeleton spotted the player` starts the fight during the approach -> `Opening cast 'sacred_flame' at 'npc_Skeleton': ok`; a later walk to within 7.5 m of the crypt skeleton -> a fight by sighting (`rv_round4_spotted_encounter.png`). |
| G-138 | S3 | Combat / UI | The target/acting rings draw over the characters - they should be hidden where they pass behind a body | Manual review 2026-09-17 ("for better 3d affect they should be obscured by characters in the places that the rings pass behind the character"). The ring is UI (G-105), which has no depth buffer. | FIXED (engine + shell): `Utils::segmentHitsCapsule` (`CapsuleOcclusionTest`: far half hidden, near half and sides visible, another body on the sight line occludes, a segment above the head does not) - a dot is dropped when the camera->dot segment crosses any combatant's body capsule (1.8 x 0.3 m, its own included). Probe L4 `rv_round4_ring_occluded.png`: the gold ring's far arc vanishes behind the skeleton, the near arc stays; the player's blue ring is cut by its own legs. |
| G-139 | S2 | Scenes / combat | A combat move into an exit region transitions the scene mid-fight | Probe L4 2026-09-17 (`rv_combat_move_move2.log`): a ground click near the farm's west edge walked the player into `back_to_town`'s region during the wolf fight -> `Beginning transition: 'farm' -> 'town'` with the encounter still running. | FIXED 2026-09-21 (engine + shell), TWO guards because either alone leaves a hole. **(1) `SceneManager::setTransitionGuard`** - a host veto at the engine's own chokepoint. EVERY route into a scene change goes through `transitionTo` (region triggers, menu buttons, the test API), so this closes the hole no matter what walked the player in; a wander or a knockback would have done it too. The engine owns the refusal, the host owns the policy: the shell refuses while `combatDirector_.inCombat()`. **(2) `PlayerTurnController::setDestinationFilter`** - refuses to spend the turn walking into a forbidden region, consulted for the requested point AND every waypoint, because a region trigger fires on ENTRY not on arrival. The shell feeds it `triggers_.exitRegions()` (the same list the compass uses) with a 0.5 u margin for the mover's arrival radius. Red-before-green: `tests/core/CombatSceneExitTest.cpp`, 5 tests. Red run with the hooks present but inert - 3 failed on BEHAVIOUR, the 2 no-guard/no-filter controls passed; green after wiring, 5/5. L4 on the SHIPPED exe (`rv_g139_l4.{log,json}`): in the farm wolf fight, `combat/player_move` toward the west exit returned `ok:false` and the scene stayed `farm`; firing `back_to_town` outright IN COMBAT logged `Transition 'farm' -> 'town' refused by the host: an encounter is running` with combat still active; after `combat/end` the SAME trigger transitioned to `town`, so the guard gates rather than breaks. Full suite green. |
| G-140 | S2 | Triggers / combat | A `start_combat` region re-fires over the corpses of a finished fight | Probe L4 2026-09-17 (`rv_aggro_before.log`): walking back through the antechamber after killing its three foes fired `encounter_antechamber` again - `Combat encounter started: 4 combatants` with three dead participants; combat sat active with no living enemy. | FIXED (shell): the `start_combat` handler drops dead/missing participants and skips the action when no living enemy remains (`start_combat (trigger 'encounter_antechamber'): no living enemy participants - skipped`, `rv_aggro_after1.log`). |
| G-141 | S1 | Render / lighting | A black exterior wall beside the town spawn (house_1's south wall, StoneBricks, x -35..-33 z 19 y 19-21), with an identical fresh cube two cells away rendering normally | User's view `rv_user_window_capture_3.png` (2026-09-17). Root cause (measured with the tone map OFF - every earlier reading went through exposure x8 + AgX, which crushed and cross-contaminated the debug views and cost most of a day): the per-fragment 5-ray sky trace (`phxSkyVisibility`, the ambient term since M3) fans its rays around the surface NORMAL, so on a vertical wall the down ray hits the ground and the normal/side rays hug the horizon; the Bricks building 13 u south blocked them and nothing sampled the upper sky. Wall sky 0.385 vs 1.0 on the cube, squared by `phxAmbientAtmos` -> 5.6x darker ambient -> black under the AgX toe; the same wall with nothing within 16 u read 1.0 (discontinuous). The same estimator caused G-135. Diagnosis: `docs/evidence/ravenmere/rv_black_wall_diagnosis_2026-09-17.md`. User's verdict: the sky-visibility model is to be rethought or removed. | FIXED at the engine level (2026-09-19): **the probe field is THE ambient source and the sky trace is deleted from GLSL.** `shaders/gi_field.glsl` (new, shared): ambient-cube probes (6 cosine lobes per probe, `GiProbeField::kLobes`), `phxAmbient` for every receiver (voxel/glass/character/foliage/grass per blade vertex), receivers keep only probes that are in air, in front of the surface and VISIBLE from it (`phxSegmentBlocked`, a two-level cube-then-micro traversal with a CPU mirror pinned by `OccupancyTraversalTest` on 20k random segments), normal-biased sampling; `gi_probe.comp` bounces off the field itself (multi-bounce by iteration), tests sun visibility per hit, rotates its 18-ray set per refresh and blends temporally (`kBlend` 0.05); default ON, `/api/debug/gi` is the kill switch; bindings 11-13 visible to vertex stages; `phxSkyGate` and `phxSkyVisibility` deleted; rules R8 (occlusion never depends on voxel size) and R9 (no receiver traces its own sky) added to `LightingPipeline.md` and enforced by `tools/lighting_doc_check.py`. New rig `tools/ambient_model_check.py` (Lighting Lab + an exterior wall pair 13 u apart + a room sealed by a 1-micro roof, noon, tone map off): RED on the trace (A1 street wall / lone wall **0.37**, A4 door wall / sealed wall 3.8 but diluted, sealed 0.079), GREEN on the field (A1 **1.00**, A2 sealed/open **0.067** = the ambient floor, A3 micro-roof/cube-roof **0.99**, A4 door wall / sealed wall **2.06** vs a 2.2 solid-angle estimate). Two bring-up traps fixed on the way: `gi_probe.comp` had NO rule in `build_shaders.bat` (its .spv was dated 2026-09-03 - the field could never have changed behaviour) and the receiver leak guard first cost 56 ms/frame on the laptop GPU until the two-level traversal (69.6 -> 17.0 ms at the lab door pose; field off 6.5 ms). Ravenmere A/B on the rebuilt probe build, same pose as the user's capture (spawn, then the eye-level look at the wall from the player's head, yaw 90 pitch 5), defect in frame both times: `rv_wall_now_*` (2026-09-17 shaders) vs `rv_wall_probes_*` (2026-09-19), montage `rv_g141_before_after_montage.png` - the wall's box mean 0.342 -> 0.472 at eye level, and the wall reads as stone bricks instead of black in the game camera. |
| G-142 | S2 | Render / lighting | Grass under a roof at noon reads ~20x brighter than the sealed room's walls | Lighting Lab, sealed room, noon, tone map off, exposure 16: floor band (grass blades) 0.041-0.044 linear vs the room's wall 0.0018 - in BOTH ambient models (trace 0.0437 / probes 0.0413), so it is not the G-141 change. Grass ambient there is the floor term like the wall's, so the light is the SUN reaching blades under the roof (the grass shadow lookup, or the cascade fit with the sun at the zenith) - not measured further. | OPEN. Measure `grass.frag` sun term vs shadowFactor under the lab roof at noon and at 10:00; suspect the blade's near-cascade lookup or the zenith fit. |
| G-143 | S1 | Render / lighting | Lighting flickers while the camera moves: shadows darken then lighten, textures go dark and recover | User, 2026-09-19, on the first G-141 build ("only happens while moving the camera"). Cause: the probe grid re-snaps its origin to the 2 u lattice as the viewer moves, but slots were addressed grid-relative, so every snap shifted the whole buffer under every surface and the temporal blend (0.05) spent ~3 s fading the wrong light out. Reproduced deterministically in the Lighting Lab: the door room's far wall after a 1 s camera trip out of the room and back read +33 % at 0.3 s, then −47..−56 % at 3 s, on each of three trips (`move_return_door.py`). Invisible at the exterior-wall pose because open-air ambient is spatially uniform. | FIXED (shaders): world-stable addressing - slot = wrapped world lattice coordinate (`phxWrapSlot`, floor division), per-slot lattice tag (lobes 2-4 .a), stale slots read invalid until rewritten and start fresh. Same trips now: within the field's own noise around the converged (6x darker, correct) value. First attempt used GLSL `%` for the wrap and was WRONG on this driver (undefined for negative operands): walls darker and unstable, the micro-roofed room leaking 2.3x; the floor-division form alone restored the green check (A1 1.00, A2 0.067, A3 0.99, A4 2.01). **User-confirmed on their own build 2026-09-20** ("looks like you fixed the issue") - the lab numbers alone never contained the user's symptom, so their eyes are what closes this row. |
| G-144 | S2 | Render / lighting | Ambient is WRONG, not merely coarse, for anything outside the probe grid (+-48 u x/z, +-24 u y around the viewer) | Measured 2026-09-20 with a STATIONARY camera by toggling `/api/debug/gi`, which takes the identical fallback path in `gi_field.glsl` as being out of grid (`tools/coverage_gap.py`, `docs/evidence/coverage_gap.{json,png}`): sealed room 2.11x too bright, 1-micro-roofed room 2.09x, door-lit far wall 5.75x, exterior wall 0.23x (4.3x too DARK - the fallback has no sun bounce); control (open-roof sunlit floor) 0.94x. Share of a real frame affected (`tools/town_coverage.py`, `docs/evidence/town_coverage.png`): town spawn 97% of geometry covered, elevated west-end vantage 80%. This is what makes brightness depend on camera position at all; the user's question, 2026-09-20. | OPEN. Fix = a far probe cascade so out-of-near-field lands in a coarse field, not on "assume open sky". **Direct sweep 2026-09-20** (`tools/ceiling_build.py` + `tools/ceiling_sweep.py`, `docs/evidence/ceiling_sweep.{json,png}`): a fixed enclosed ceiling, camera flying straight up beneath it, the SAME surface filling frame throughout. Coverage ends exactly 22 u above the viewer - the value derived from the grid arithmetic before measuring - covered 0% at 24 u and above, 100% at 22 u and below. Where uncovered the surface reads the fallback EXACTLY (field on vs off identical to 4 dp: 0.0734/0.0734, 0.0651/0.0651, 0.0610/0.0610). Where covered the field departs: x1.02 at 22 u, x1.13 at 20, x1.25 at 18, x1.55 at 14, x1.87 at 6. TWO CORRECTIONS to my earlier account. (a) I predicted the no-answer reference would be flat with distance; it is not (0.0734 -> 0.0229) because `phxAerialPerspective` is applied per fragment by distance - I forgot haze. (b) The boundary is NOT a hard step: the field cross-fades into the fallback over the grid's outer 4 probes (8 u), which is why the ratio ramps instead of jumping. So this gap does NOT produce a moving seam or the flicker of G-143; it quietly lights distant enclosed geometry wrong. |
| G-146 | S1 | Render / perf | The G-141 ambient model costs 39-52 ms/frame in the TOWN, roughly half the frame | Measured 2026-09-20 with `tools/ambient_cost.py`, which reads the engine's own per-pass GPU timings (`GET /api/debug/gpu_scopes` - it already reported a `GI Probes` scope; no new instrumentation was needed). Field ON vs OFF, three fixed poses, noon, medians. spawn 132.1 vs 88.5 ms, wall 87.7 vs 35.3, west 65.2 vs 26.2. The probe COMPUTE pass is only 5.1-5.3 ms (10-13%); the rest is per-fragment/per-vertex receiver work. **Grass alone is +20 to +22 ms in every pose, including one with almost no grass on screen**, because `grass.vert` calls `phxAmbient` per BLADE VERTEX (24 per blade), so it scales with blade count, not screen coverage. My own prediction before this run was "probe compute 2-3 ms, per-fragment 7-8 ms": the SPLIT direction was right, the magnitudes were wrong by 2x and 5x because the only cost I had measured was in the nearly-empty Lighting Lab and I quoted it as the cost. | PARTLY FIXED 2026-09-20 — item (1), grass, is done. `phxAmbientUp` in `gi_field.glsl`, called from `grass.vert`: drops the X and Z lobes (EXACT for an up normal, their weights are zero) and the per-neighbour visibility trace (the one approximation). Grass ambient 20.7/20.1/22.3 ms -> 0.9/-0.4/1.0 ms; whole frame 132.1/87.7/65.2 -> 113.7/68.7/43.9 ms at the three poses. Gates all held: outdoor grass pixels -0.09% to +2.03% (`grass_look_{before,after}.json`), indoor-grass floor band 0.1% across all three enclosed lab rooms, `ambient_model_check.py` green on all five invariants (`ambient_grassfix.json`). STILL OPEN, ranked by measured size: (1) the per-fragment path in `voxel.frag`, now the largest at 8-20 ms, the trilinear fetch plus `phxSegmentBlocked` (standard DDGI replaces the per-fragment traces with a per-probe occlusion lookup); (2) the probe pass, 5.6 ms, of which skipping buried probes before tracing wins at most half. NOT YET MEASURED: the cost of the PREVIOUS model for comparison. A shader-only revert to 16fb50ca produces a hybrid that existed at no point in history (the engine still enables the field and sizes the buffer for 6 lobes) and two poses rendered nothing; a real before/after needs a full engine build at the old commit. |
| G-147 | S2 | Render / camera | Zoomed fully in, the player's own body clips into the view | User feedback 2026-09-21: "when fully zoomed in so the camera is basically first person, i dont want to see the player character cliping into the view". `MmoRig` already switches to an eye view under 1 u of boom (`kFirstPersonBelow`) and the boom is ALSO shortened by wall collision, so the camera lands inside the body either way and the mesh crosses the near plane. Nothing suppressed the owner: the only per-character skip was the F5 debug-overlay one. | FIXED 2026-09-21 (engine + shell). `Utils::pointInsideCapsule` (the same maths as `segmentHitsCapsule` with a degenerate segment, one implementation to be wrong) and `RenderCoordinator::setCameraOwnerProvider`: while the camera is inside the owner's body capsule it is dropped from the MAIN pass only, so the player still casts a shadow. Geometric, so the wall-pushed case needs no special handling. A PROVIDER not a pointer - the shell reassigns `playerCharacter_` on every scene load, and my first attempt stored the pointer once at startup and the L4 measured it never firing. Red/green by MUTATION (the predicate forced to always-false = the original defect): 2 of the 8 capsule tests fail, restored 8/8. L4 on the SHIPPED exe (`rv_g147_l4.{log,json}`, montage `rv_g147_before_after.png`): `camera_owner_hidden` false -> true -> false as the camera moves to the eye point and back, and the frames show the body present, gone with a clean view, present again. `get_render_stats` now reports the flag so this is assertable rather than eyeballed. |
| G-148 | S2 | UI / action bar | The action bar shows long text strings; it should show icons, with mouse-over giving the text | User feedback 2026-09-21: "player action bar should show icons not long strings. each spell needs an icon. we should be able to generate that. mouse over of a spell or action should then result in text information". Today `hud_action_bar`'s repeater item is a `button` bound to `item.label`, so a row reads "Guiding Bolt (2)". | FIXED 2026-09-21 (engine + shell + generated art). **Icons:** `tools/gen_spell_icons.py` reads `resources/spells/*.json` and emits one 64x64 pixel-art PNG per spell plus Attack/End Turn (38 files), drawn on a 16x16 grid at 4x nearest to match the voxel look, coloured from the spell's own damage type / heal / school. **Widget:** `UIButton` gained `icon` / `iconBind`; with an icon it draws the picture instead of its label, dims when disabled, and falls back to the label if the PNG fails to load. **Tooltips** (the piece that was genuinely missing): any widget can carry `tooltip` / `tooltipBind`; `UIWidget::handleHover` got a default box test so every type is a hover target, `UIButton` no longer requires `enabled` to register hover (a greyed row is the one you point at to learn WHY), and `UISystem` draws the deepest hovered tooltip LAST, at the pointer, clamped in-canvas. The shell builds the text from `SpellRegistry` - level/school/range, damage or healing, concentration, the wrapped description, and the block reason when the row is actually unavailable. **Also:** panels can now `autoWidth`, because 52 px icon slots left the authored 1000 px plate nearly empty. **Also:** the shell now runs a per-frame HOVER pass while playing - it deliberately does not call `handleInput` during gameplay (the HUD would eat gameplay keys), so hover was never updated and the first L4 measured the engine's live hover as "" with the real cursor on a slot. Red/green by MUTATION (icon + tooltip record binding removed, the `enabled` hover gate restored): 2 of the 5 new tests fail, 4 assertions on the icon/tooltip pair and 2 on the hover pair; restored 7/7. The panel-tooltip test went red for real before `UIPanel::handleHover` set its own flag. Removing one PNG reds the coverage test by name. **L4 on the SHIPPED exe** (`rv_g148_l4.{json,log}`, montage `rv_g148_before_after.png`): sweeping the bar with `ui_hover` finds 3 DISTINCT tooltips carrying real spell data ("Guiding Bolt (2) / Level 1 - Evocation - 120 ft / 4d6 Radiant / ..."), off the bar returns "", and with the REAL Windows cursor moved onto a slot the engine's own live hover reports that tooltip and the capture shows it in frame. The before frame is the same exe at the same pose with only the authored widget reverted. Harness hook: `ui_hover {x,y}` injects, `ui_hover {}` reads the live state. |
| G-149 | S3 | Test API | The shipped harness cannot scroll the mouse wheel or set the camera zoom | Hit while L4-ing G-147: `inject_input` handles keys only, and `set_camera` only detaches to an absolute pose, so the zoom-to-first-person INPUT path cannot be driven from a harness. The G-147 L4 had to place the camera at the eye point directly, which exercises the same geometric test but not the rig's boom collapse. | OPEN. Add a scroll to `inject_input` and/or a rig-zoom parameter to `set_camera`. The HOVER half of this gap is closed (G-148 added `ui_hover`, which injects a pointer position or reads the live one); the wheel and zoom remain. |
| G-145 | S3 | Engine / jobs | `clear_region` can crash the editor during finalization | Hit 2026-09-20 building a lighting rig: `clear_region` over x -40..80, y 17..31, z -32..-25 logged "background complete, awaiting finalization" then died. `docs/evidence/crash_clear_region_20260920.txt` (copied out of `crashes/`, whose .dmp files are not committed): ACCESS_VIOLATION reading 0x4c (a null-ish `this` or member) at phyxel.exe+0x240ad0, on the job thread. Reproduced once; the preceding smaller clears on the same region succeeded. | OPEN. Symbolise the dump against the matching build and guard the finalization path; unrelated to lighting. |
| G-21 | S3 | Authoring tools | Find ground height at a spot | `get_terrain_height` returns the TOP voxel of the column — canopy (y 25-29 across the whole strip west of the street) or roof (24 inside the tavern) — so it cannot answer "where does an NPC stand" in a built town | Use furniture/door positions from `list_placed_objects` (593 KB, needs offline parsing) | A `get_walkable_height {x,z}` (or `flora-blind` + interior-aware option) | OPEN 2026-09-08 |

## Session log

- **2026-09-19 (G-143: camera-motion flicker)** — User's report on the new ambient: "shadows will darken then lighten again, sometimes textures go dark and go back to normal. only while moving the camera". Grid-relative probe slots shifting under surfaces on every 2 u origin snap; fixed with world-stable (scrolling) addressing + slot tags. Red/green in the lab door room (+33 %/−56 % per trip → noise). A first version wrapped with GLSL `%` and produced wrong slots (undefined for negatives) — caught by the ambient check going red (A1 0.75, A3 2.3), bisected in two shader builds. Both Ravenmere trees rebuilt.
- **2026-09-19 (G-141: the ambient model replaced)** — The user's call after the black-wall diagnosis: "the sky-visibility logic is clearly shit. we need to rethink it completely or get rid of it", and "a microcube can block light just as well as a full size cube". Done: the probe field is the one ambient source (ambient-cube probes, multi-bounce, receiver-side visibility, rotated rays + temporal blend), the per-fragment sky trace is deleted, rules R8/R9 written and enforced, `tools/ambient_model_check.py` red on the old model / green on the new (numbers in G-141), `gi_probe.comp` finally has a build rule (its compiled shader was 16 days stale), two-level occupancy traversal with a CPU mirror + equivalence test. Cost on this laptop (RTX 1000 Ada, lab door pose): field off 6.5 ms, on 17.0 ms. Residuals logged in `LightingPipeline.md` §8: interiors beyond the 96 u grid read as open sky; a wall lit only through a 1-wide doorway drifts ±25% over seconds at blend 0.08 (0.05 shipped, not re-measured); G-142 grass under a roof at noon.
- **2026-09-08** — project scaffolded from a 5-scene draft (`create_project.py Ravenmere`),
  rpg genre linked (after G-11), Release editor launched on the project (G-10). Town generated
  by the ENGINE: `POST /api/settlement/build {era:medieval, tier:town, seed:7, position:(-40,16,-20),
  width:100, depth:70}` → async job 1000000, complete in 146 s: 14 buildings (2 taverns, 4 halls,
  general store, bakery, blacksmith, butcher, 3 crofts), 8 streets, well, 4 market stalls, 28 yard
  props, 301 placed objects; saved to `worlds/town.db` (27 chunks). Cast placed from measured
  positions (signs = doors, tavern bar, stalls). GAMEPLAN filled; tracker: design_brief + world
  validated (world L4 in the editor), perf_target honestly RED (9 FPS). Standalone built
  (Release, 8 min, engine compiled in-tree — G-23) and the API playthrough
  (`docs/evidence/ravenmere/rv_playthrough.py`) started against `Ravenmere.exe --test 8104`.
  Boot to menu: 50 s. Town scene load in the shipped build: 1.9 s, 12 authored NPCs.
- **2026-09-08 (afternoon) — five probe runs, each one stopped by a different engine defect,
  each defect fixed the same day.** Run 1: choices hidden (G-26 globalStory never applied) +
  pause menu immune to injected Escape (G-27). Run 2: Bram joined twice (G-29 level-triggered
  keys) + the dire wolf fled forever (G-31). Run 3: injected keys immortal outside the input
  pump (G-33 — the real cause of G-27). Run 4: digits ignored during the typewriter (G-35,
  harness-side). Run 5: **first fights played end to end in the shipped build** — Space ended
  9 turns, stat blocks live (`dire-wolf AC 14 37 hp`), the alpha fled 3× then ESCAPED and the
  encounter resolved; then the wolves killed the cleric and the game did not notice (G-38: no
  game-over state existed, the corpse kept fighting). Fixed: GameOver screen state + JSON +
  `show_game_over`, dead-player turn skip, encounter ends on player death. Also shipped today:
  `monsterId` in game.json (G-05), dialogue skill checks (G-03), `reveal_objective` (G-06),
  End Turn (G-02), `invulnerable` standalone (G-34). Evidence: `docs/evidence/ravenmere/`
  (`rv_run2_stalled.log`, `rv_run3_stuckkeys.log`, `rv_run4_typewriter.log`,
  `rv_run5_playerdeath.log`, `rv_probe_stdout_run*.txt`). Unit test for the escape rule:
  `tests/core/CombatAISystemEscapeTest.cpp` (build pending).
- **2026-09-08 (evening) — runs 6 and 7 reached the barrow.** Run 6 played the whole first
  half of the game in the shipped build: accept → Persuasion check FAILED (roll 7) → Maera → Bram →
  cellar rats (4 rounds, `combat_victory`, 300 XP → LEVEL UP 2) → Hobb → farm wolves (7 rounds, won,
  Bram killed) → tracks → Oswin → Reeve → Wren joins (700 XP) → Oswin joins → barrow antechamber
  (2 rounds) → grimoire → crypt: a real spell duel (`scorching_ray`, `magic_missile`, an NPC
  `cure_wounds` heal) and then Wren's turn looped forever (G-42). Found on the way: Bram was
  resurrected for free by the next scene load (G-41), the shipped RPG runs a creative-mode
  inventory (G-37), empty-`then` triggers are rejected (G-40), re-entry spawns at the scene
  default (G-36). Fixed the same evening: AI stall guard + TurnActor no-progress stop (unit-tested,
  4/4), party death bookkeeping + long-rest revive. Run 7: Persuasion PASSED (roll 17) → both
  branches of a dialogue skill check observed live; the run then missed the cellar exit region by
  0.5 u (probe tolerance) — regions widened. The unit suite for this work:
  `CombatAISystemEscapeTest` + `TurnActorTest` + `PlayerTurnControllerTest` + `CombatDirectorTest`
  = 37/37 passing on the tree (`build/tests/Debug/phyxel_tests.exe`).
- **2026-09-08 (night) — RUN 13: THE GAME IS PLAYED START TO VICTORY IN THE SHIPPED BUILD.**
  Runs 8–12 each fixed one more thing (trapdoor placement, cellar exit region, a pre-made with
  real AC/abilities after two more deaths to the wolf pack (G-43/G-44/G-45), the relic dais
  step (G-46), blind choice indices (G-47)). Run 13, 690 s over the `--test` API of
  `build/Release/Ravenmere.exe`: menu → Reeve (accept, `[Persuasion DC 12]` passed) → Maera →
  Bram joins → cellar rats (6 rounds) → Hobb pays 20 crowns → farm wolves (8 rounds, cleric
  at 45/100) → Dunstan pays → tracks → Oswin → Reeve (tracks) → Wren joins → Oswin joins →
  barrow antechamber (4) → grimoire → crypt boss (8, Hollow Priest dead) → relic → home →
  `Trigger 'win' fired (objective_complete main_6)` → +75 XP, 1275 XP level 3, profile saved,
  victory screen. Evidence: `rv_run13_VICTORY.log`, `rv_probe_stdout_run13_victory.txt`,
  `rv_playthrough_evidence.json`, `rv_victory_screen.png`. Every fight is the engine's D&D
  ruleset; nothing was hand-assembled. **CORRECTION (same night, from the user's first play):**
  the TOWN was not in any of these runs — the shipped build regenerated a flat field because the
  town scene kept an inline `world` block (G-48). The quests, dialogue and fights above are real;
  the "walked through the tavern door" reading of the steering was an inference over an open
  field. The town exists in `worlds/town.db` and loads only after the fix below.
- **2026-09-08 (late) — RUNS 14–25 AGAINST THE REAL TOWN (user asked for the automated playthrough
  "now that you actually have the town").** What the real geometry did to a probe that had reached
  victory in the field: Reeve, Maera, Bram (from the tavern doorway), the cellar hatch (now outside
  behind the tavern), the rats fight (5 rounds) and the return to town all pass in run 25; the run
  then dies INSIDE the tavern — it cannot reach Hobb behind the bar (80 s of steering from the
  doorway fails; the interact then talks to the following Bram, G-58), and `navgrid_path` from the
  doorstep to the street returns `found:false, nodesExpanded:2` because the doorstep column reads
  the lintel as its floor (G-60) and every neighbour is `nearWall` (G-54). Direct steering then
  slides along the hall's wall and enters the tavern through a shuttered window (G-57), where it
  stays. New rows this stretch: G-53 (nav grid built on the menu world — FIXED), G-54, G-55, G-56,
  G-57, G-58, G-59 (no alleys between buildings on a block), G-60. FPS along the walk (shipped
  Release exe, RTX 1000 Ada laptop, third-person camera fixed at yaw −135°): 50–56 at the west edge
  looking away from the town, 14–19 on the main street / by the hall looking into it, 13.8–14.5 in
  the tavern doorway, 7.5 on the frame after the scene loads, 41–48 in the cellar, 170–250 on
  transition frames. The frame cost tracks how much of the town is in view, not where the player
  stands. Evidence: `rv_run25_realtown_stuck_in_tavern.log`, `rv_probe_stdout_run25.txt`, the
  `navgrid_cell` dump of x −29..−21 × z −6..−11 (roof cells y=23 "walkable", doorstep y=21).
  **Conclusion:** the town is playable on foot for a human (doors work, the street connects
  everything), but nothing that uses the nav grid — NPC routines, companions, the harness — can
  enter a building. G-54 + G-60 are the gate for the next full run; the probe is the regression
  test for them.
- **2026-09-09 (walkability loop, afternoon)** — G-77 door height (realizer lintel above the opening + 5 cm controller head clearance) → the first run ever to enter a generated building by its door (run 33). Then the loop found, in order: G-78 (fence/wall alley pinch: fence planes < 2 cells from a neighbour wall dropped, surviving runs pulled back from dropped corners), G-79 (per-waypoint arrival radius from the graph's slack), G-80 (hatch floor plate on the walking line; moved + harness drift fixed), G-81 (unclamped frame delta in the shipped runtime → 7 s step through walls into an endless fall; clamp + kinematic substep + input release), G-82 (re-entry restores the camera, log garbled). Harness fixes: waypoint tolerance 0.2 m, net-zero calibration, scene-change abort, walk traces. **Run 36 = the first complete playthrough of the ENGINE-GENERATED town** (menu → reeve → Maera → tavern → cellar → Hobb → farm → wolves → tracks → Oswin → Reeve → Wren → Oswin joins → barrow → priest → relic → finale, 1140 s, 1 stall). Regen #8 (corner trim) + run 37 in flight.
- **2026-09-10 (walkability loop, morning)** — Increment 1 L4: the `walkability gate` unit in `build_settlement` reports `40 walkable, 0 blocked over 40 routes (14 front doors, 1 skipped)` on regen #13 after three probe-artefact fixes found by instrumenting the gate itself (seated endpoints; the flood band must enclose every endpoint, the recorded footprint w/d does not always match the marker's side). New tools: `GET /api/walk/probe` (the gate's flood from a point, first-step verdicts) and gate-time first-step diagnostics for 1-cell starts. Runs 38/39: the player died to the farm wolves twice (G-83; harness now aborts on death). Run 40: an overlapping regen corrupted the shipped world's WAL and the runtime spawned the player into a 0-chunk world (G-84). Ops rule: never overlap a regen with a probe run on this laptop (editor 4 GB + shipped exe 4.7 GB). **Run 41 (regen #13): the third complete playthrough - victory at 994 s with ZERO stalls, no involuntary transitions (evidence json run 41).** **Late morning:** regen #15 gate 44/44 (G-85 door finding); increments 5 (WorldHealth self-check + `world_health`) and 6 (`playtest/defects.jsonl`) shipped and L4 in run 43, which found G-86 (Gerrit on a canopy) before any walking; G-86 fixed (NavGraph-based relocation). Runs 42 died to the wolves (3/5), run 43 stalled on a harness beeline (harness re-plans now). **Midday:** G-86 fixed (NavGraph-based NPC relocation; run 44 `world_health` 14/14 on both town entries); run 44 = 4th complete playthrough (victory 1090 s, zero stalls); increment 4 partial - one `AgentProfile.h` (2/17/4 micro) for the gate and the graph, BuildingHarness parity goldens re-pinned for G-77's lintels (they had been red since 2026-09-09 - the suite was not in the routine filter).
- **2026-09-10 (walkability loop, afternoon)** — Regen #16 with the extended world: gate 44/44, 14 residents adopted. The markers made three more generator/loader defects visible: the storeroom sconce is present but the room reads dim (G-89 measured: 30.0 vs 22.0 luminance with the sconce off — control shot), nine rooms across the town lost their sconce to the floor-cell fight (G-89 root cause → hung-mount rule), a barrel stood on the trapdoor cell (G-90 → keep-clear boxes from trigger regions), and every load re-placed the props (G-91 → loader idempotence). All four fixed at the engine level with red/green tests (112 green; hung rule mutation-checked). L4 for all = regen #17 + run 49.
- **2026-09-10 (walkability loop, evening)** — Regens #17–#20 chased the marker chain to the bottom: the sconce hung-mount rule and keep-clear boxes held from #17 on (12 → 0 lantern misses, 4 → 0 dark rooms, hatch cell clear), but "already present" hid four more engine defects in a row — the micro pose never persisted (G-92), an inline world block regenerated over the saved town at the post-build reload (G-93, a G-48 repeat; guard added), and `createChunk` created an EMPTY chunk over a saved-but-unloaded one and the save wrote it back — the tavern chunk went 1.88 MB → 775 bytes (G-94; createChunk now loads saved chunks, the loader brings a marker's chunk in first). Regen #20 is clean end to end: gate 44/44, 14/14 residents, one hatch seated on the slab and visible at noon by its own sconce (40.9 vs 21.7 luminance control), one waystone, full-size chunks. Shipped rebuild + run 49 next.
- **2026-09-10 (run 49, shipped build)** — Town chain green end to end on the regen #20 world: `world_health` 14/14 on both town entries, `Transition markers: 0 placed, 2 already present` with `121/121` / `145/145` intact checks, trapdoor taken with E, Bram/Hobb/Wren legs arrived, first farm visit + wolves + Dunstan paid. Regressed on the SECOND farm visit: the G-93 guard refused regeneration but loaded nothing, so the farm came up with 2 of 9 chunks (see G-93) — the harness wedged at Oswin and never reached the barrow. Fixed (refused path loads the region; loader-level test, mutation-checked). Run 50 pending.
- **2026-09-10 (solution-auditor on G-89..G-94)** — Verdict: the six mechanisms are REAL (code + falsifiable tests + log lines and DB bytes re-verified independently), with three corrections taken: (1) the regen-16 sconce-miss count was NINE rooms, not twelve (ledger + plan corrected); (2) evidence hygiene — the harness wrote every run's engine log to `rv_runA.log`, so run 50 overwrote run 49's log while the audit read it; engine logs are now per-run (`rv_run_<timestamp>.log`); (3) the harness kept recording for 20 minutes after the run-49 wedge, labelling never-started fights `resolved:true` — a fight now needs rounds > 0, and a scene leg that does not land in its scene ends the run (`expect_scene`). One auditor finding was wrong: the farm scene DOES carry an inline `world` block (`game.json`: Flat seed 77), so the G-93 refused-path fix is the right mechanism for the second-farm-visit wedge; run 50 is its L4.
- **2026-09-11 (run 51, shipped build — CLEAN)** — After G-95 (both DB-load paths now finalize chunks through `finalizeLoadedChunk`): the hardened harness completed the whole main line in 1,313 s with no `leg_failed`: `world_health` 14/14 on both town entries, trapdoor → cellar → rats fought (4 rounds) → Hobb paid → farm → wolves fought (9 rounds) → Dunstan paid → tracks → Oswin → town → Reeve → Wren → farm again (Oswin ARRIVED) → barrow → antechamber fought (3) → grimoire → crypt fought (6) → relic → farm → town → finale. Engine log `rv_run_20260911_094440.log`, harness `run51_harness.log`. Open from this loop: G-96 (intermittent tavern-door wedge, run 50 only), G-97 (walk-probe diagnostic unusable), G-83 balance, G-84, G-88 generator-side world extent.
- **2026-09-17 (fourth manual pass)** - G-136 combat moves path on the NavGraph with a voxel-surface pick (four farm clicks within 0.27 m), G-137 fights start from clicks / sightings / an armed opening spell (`Core::EncounterInitiator`; hostility from the authored enemy side, not the AI profile - that mis-marked the companions), G-138 ring dots hidden behind bodies, G-140 corpse re-fire filtered; open: G-135 low-poly shadows (need a location), G-139 exit regions fire mid-combat.
- **2026-09-16 (third manual pass)** — G-131 dead-NPC prompt, G-132 window-resize placement (engine `UISystem` placement), G-105 ring finally drawn (via UI markers; bracket retired), G-133 character sheet (C), and G-130 the New Game crash: reproduced 3/3 on the 16:40 build, 0/5 on the 17:07 build, NOT fixed — `Core::CrashHandler` + shipped PDBs now turn the next one into a symbolized stack. Also found: `GameSettings` load dropped new default actions (fixed), data screens did not hide the HUD (fixed).
- **2026-09-16 (second manual pass)** — G-128 lean sign + run-strafe clips, G-121 plate hover, G-129 New Game restarts the run; all three L4 on the probe build.
- **2026-09-16 (engine first: G-117 compass)** — compass strip with exits/locations as points of interest; L4 headings match; one unreproduced device loss logged as G-127.
- **2026-09-16 (engine first: G-118 + G-114 cache)** — run-strafe body lean (numeric L4); the atlas BC7 cache made content-keyed and decode-skipping: boot 40.6 s → 3.9 s to the menu.
- **2026-09-16 (engine first: combat batch G-121/G-122/G-123 + G-120 partial)** — capsule pick, stacked plate column with the bracket around it, drag-orbit in combat, prompts hidden in combat; L4 in the barrow fight. New: G-126 plates over HUD panels.
- **2026-09-16 (engine first: G-115)** — Loading screens now render (SceneManager presents a frame before the load; shell holds until chunks settle); L4 on New Game and town→farm.
- **2026-09-16 (engine first: G-124)** — Patrol/wander/follow behaviours moved onto the NavGraph with a red door-routing test; live in the town: stuck replans 131–264 → 16 per 90 s, exhausted searches → 0, wanderers walking 33–98 m. One body still pinned (G-125).
- **2026-09-16 (manual review, "getting close")** — Eleven items from the user's playthrough triaged into G-114..G-124: boot splash (atlas build), the never-rendered loading_screen transition style, an intro, compass/minimap (G-51), run-strafe animation, the unsignposted farm road, target visibility + prompts in combat, the chest-circle pick, nameplate/readout/bracket collisions, no camera turn in combat, and NPC patrols still on the legacy 2.5D grid.
- **2026-09-15 (WoW third-person controls, G-111)** — Researched WoW's default bindings/camera and made them the engine's `third_person` default (MmoScheme + MmoRig, follow/steer in the controller, collision boom, wall-aware zoom); L4 on the probe build hit every prediction. Found on the way: G-112 (sub-frame injected taps lost), and two tooling traps that cost a cycle each — `localhost` = 2 s per API call (use 127.0.0.1) and the town spawn facing the street's north wall (W runs 2.7 m into it; turn first). Town frame time measured at 126 ms (8 FPS) on this laptop, unchanged by the build.
- **2026-09-15 (click-to-move, increment 5)** — G-75 built at the engine level (`Core::ClickToMove` on the NPCs' NavGraph, pointer-driven rulesets keep the cursor free, LMB never swings) and L4'd on the probe build: API walk / ground click / NPC click-and-talk / WASD cancel / off-viewport refusal all as predicted. Found on the way: G-108 (dialogue panel clipped its choices + health overlap — lint had never run with a dialogue open; fixed), G-109 (screen-to-world unprojection at NDC z=0 = infinity under reverse-Z; fixed in both pickers), G-110 (`screen_of` ok off-screen; open). Lesson: the engine logger formats plain `{}` only — `{:.2f}` prints literally and shifts the arguments.
- **2026-09-15 (run 53, probe build — CLEAN, VICTORY)** — First full playthrough with the whole UI/combat slice in: 1543 s, `ui_lint` 0 on both town entries, `world_health` 14/14, rats/wolves/antechamber/crypt fought (5/8/3/6 rounds), farm3 leg passes on the body-radius region (G-107 L4), wallet credited 30 gp, finale on the victory screen. Increments 1–4b committed as 5f4809a3.
- **2026-09-15 (action bar L4, probe build)** — The bar as shipped after G-106's first build had blank buttons and swallowed clicks (two engine defects, see G-106); fixed red-before-green, re-verified live: labels fit, End Turn routes through the handler, Attack correctly disabled once the pick had spent the action. Run 53 (probe) launched for G-107.
- **2026-09-15 (run 52, probe build)** — Clean to the relic with `ui_lint` 0 on both town entries (the HUD layout pass, the item model and the targeting slice all in), then `leg_failed farm3`: the player 8 cm short of the exit region (G-107, body-radius regions). The stale `gold_crown ×30` in the relic readout was run 51's saved profile (harness wipes it now; legacy coin items migrate to the wallet).
- **2026-09-11 (manual test: UI + combat controls)** — User's findings: characters clipped into the floor (→ G-98 root-caused and fixed at the engine level, G-99 open), HUD text overflowing panels and panels overlapping, a Minecraft hotbar with 64-stacks instead of a BG3 action bar, no visible way to select a target or attack. Workstream plan: `docs/game-production/CombatUiBg3.md`.
