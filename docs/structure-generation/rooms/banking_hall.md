# banking hall — Room Data Sheet

> Schema: [`README.md`](README.md). Used by: bank / counting_house. Reuses the `bank` archetype sheet. **The public face of the access gradient.**

## Function
Receive customers + transact — the public floor (T0). The **public ↔ staff boundary**.

## Required fixtures (→ #17)
- A **counter + grille** (the public/staff boundary).

## Typical fixtures (→ #16)
- A bench, a counter scale, barrable shutters.

## Service
- **One** controlled street entrance; **barrable** (against a run/riot); the grille has **no human-passable gap**.

## Adjacency
- Public — fronts the entrance; the **counter/grille separates** it from the staff zone (counting room); **no public path beyond** to the vault.

## Dimensions
- `to_ground`.

## Function testers
- **F1** A counter + grille separating public from staff (no human-passable gap).
- **F2** One public entrance; barrable.
- **F3** No public path beyond the counter (to the vault).

## Grounding ledger
- Counter/grille boundary + barrable — REUSE `bank` (cited); size `to_ground`.
